---
summary: "CLI reference for `opensoul uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `opensoul uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
opensoul uninstall
opensoul uninstall --all --yes
opensoul uninstall --dry-run
```
