---
summary: "CLI reference for `opensoul docs` (search the live docs index)"
read_when:
  - You want to search the live OpenSoul docs from the terminal
title: "docs"
---

# `opensoul docs`

Search the live docs index.

```bash
opensoul docs browser extension
opensoul docs sandbox allowHostControl
```
