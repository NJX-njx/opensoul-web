---
summary: "CLI reference for `opensoul sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `opensoul sessions`

List stored conversation sessions.

```bash
opensoul sessions
opensoul sessions --active 120
opensoul sessions --json
```
