import{_ as e,o as i,c as n,ai as l}from"./chunks/framework.CsYGeNYI.js";const u=JSON.parse('{"title":"会话工具","description":"","frontmatter":{"read_when":["添加或修改会话工具时"],"summary":"用于列出会话、获取历史记录和发送跨会话消息的智能体会话工具","title":"会话工具","x-i18n":{"generated_at":"2026-02-03T07:46:54Z","model":"claude-opus-4-5","provider":"pi","source_hash":"cb6e0982ebf507bcf9de4bb17719759c2b6d3e519731c845580a55279084e4c8","source_path":"concepts/session-tool.md","workflow":15}},"headers":[],"relativePath":"zh-CN/concepts/session-tool.md","filePath":"zh-CN/concepts/session-tool.md","lastUpdated":null}'),a={name:"zh-CN/concepts/session-tool.md"};function o(d,s,t,c,p,h){return i(),n("div",null,[...s[0]||(s[0]=[l(`<h1 id="会话工具" tabindex="-1"><a class="header-anchor" href="#会话工具">#</a> 会话工具</h1><p>目标：小型、不易误用的工具集，使智能体能够列出会话、获取历史记录并向另一个会话发送消息。</p><h2 id="工具名称" tabindex="-1"><a class="header-anchor" href="#工具名称">#</a> 工具名称</h2><ul><li><code>sessions_list</code></li><li><code>sessions_history</code></li><li><code>sessions_send</code></li><li><code>sessions_spawn</code></li></ul><h2 id="键模型" tabindex="-1"><a class="header-anchor" href="#键模型">#</a> 键模型</h2><ul><li>主直接聊天桶始终是字面键 <code>&quot;main&quot;</code>（解析为当前智能体的主键）。</li><li>群聊使用 <code>agent:&lt;agentId&gt;:&lt;channel&gt;:group:&lt;id&gt;</code> 或 <code>agent:&lt;agentId&gt;:&lt;channel&gt;:channel:&lt;id&gt;</code>（传递完整键）。</li><li>定时任务使用 <code>cron:&lt;job.id&gt;</code>。</li><li>Hooks 使用 <code>hook:&lt;uuid&gt;</code>，除非明确设置。</li><li>Node 会话使用 <code>node-&lt;nodeId&gt;</code>，除非明确设置。</li></ul><p><code>global</code> 和 <code>unknown</code> 是保留值，永远不会被列出。如果 <code>session.scope = &quot;global&quot;</code>，我们会将其别名为 <code>main</code> 用于所有工具，这样调用者永远不会看到 <code>global</code>。</p><h2 id="sessions-list" tabindex="-1"><a class="header-anchor" href="#sessions-list">#</a> sessions_list</h2><p>将会话列为行数组。</p><p>参数：</p><ul><li><code>kinds?: string[]</code> 过滤器：<code>&quot;main&quot; | &quot;group&quot; | &quot;cron&quot; | &quot;hook&quot; | &quot;node&quot; | &quot;other&quot;</code> 中的任意一个</li><li><code>limit?: number</code> 最大行数（默认：服务器默认值，限制如 200）</li><li><code>activeMinutes?: number</code> 仅在 N 分钟内更新的会话</li><li><code>messageLimit?: number</code> 0 = 无消息（默认 0）；&gt;0 = 包含最后 N 条消息</li></ul><p>行为：</p><ul><li><code>messageLimit &gt; 0</code> 获取每个会话的 <code>chat.history</code> 并包含最后 N 条消息。</li><li>工具结果在列表输出中被过滤；使用 <code>sessions_history</code> 获取工具消息。</li><li>在<strong>沙箱隔离</strong>的智能体会话中运行时，会话工具默认为<strong>仅生成的可见性</strong>（见下文）。</li></ul><p>行结构（JSON）：</p><ul><li><code>key</code>：会话键（字符串）</li><li><code>kind</code>：<code>main | group | cron | hook | node | other</code></li><li><code>channel</code>：<code>whatsapp | telegram | discord | signal | imessage | webchat | internal | unknown</code></li><li><code>displayName</code>（如果可用的群组显示标签）</li><li><code>updatedAt</code>（毫秒）</li><li><code>sessionId</code></li><li><code>model</code>、<code>contextTokens</code>、<code>totalTokens</code></li><li><code>thinkingLevel</code>、<code>verboseLevel</code>、<code>systemSent</code>、<code>abortedLastRun</code></li><li><code>sendPolicy</code>（如果设置的会话覆盖）</li><li><code>lastChannel</code>、<code>lastTo</code></li><li><code>deliveryContext</code>（可用时的规范化 <code>{ channel, to, accountId }</code>）</li><li><code>transcriptPath</code>（从存储目录 + sessionId 派生的尽力路径）</li><li><code>messages?</code>（仅当 <code>messageLimit &gt; 0</code> 时）</li></ul><h2 id="sessions-history" tabindex="-1"><a class="header-anchor" href="#sessions-history">#</a> sessions_history</h2><p>获取一个会话的记录。</p><p>参数：</p><ul><li><code>sessionKey</code>（必填；接受会话键或来自 <code>sessions_list</code> 的 <code>sessionId</code>）</li><li><code>limit?: number</code> 最大消息数（服务器限制）</li><li><code>includeTools?: boolean</code>（默认 false）</li></ul><p>行为：</p><ul><li><code>includeTools=false</code> 过滤 <code>role: &quot;toolResult&quot;</code> 消息。</li><li>以原始记录格式返回消息数组。</li><li>当给定 <code>sessionId</code> 时，OpenSoul 将其解析为相应的会话键（缺失的 id 会报错）。</li></ul><h2 id="sessions-send" tabindex="-1"><a class="header-anchor" href="#sessions-send">#</a> sessions_send</h2><p>向另一个会话发送消息。</p><p>参数：</p><ul><li><code>sessionKey</code>（必填；接受会话键或来自 <code>sessions_list</code> 的 <code>sessionId</code>）</li><li><code>message</code>（必填）</li><li><code>timeoutSeconds?: number</code>（默认 &gt;0；0 = 即发即忘）</li></ul><p>行为：</p><ul><li><code>timeoutSeconds = 0</code>：入队并返回 <code>{ runId, status: &quot;accepted&quot; }</code>。</li><li><code>timeoutSeconds &gt; 0</code>：等待最多 N 秒完成，然后返回 <code>{ runId, status: &quot;ok&quot;, reply }</code>。</li><li>如果等待超时：<code>{ runId, status: &quot;timeout&quot;, error }</code>。运行继续；稍后调用 <code>sessions_history</code>。</li><li>如果运行失败：<code>{ runId, status: &quot;error&quot;, error }</code>。</li><li>通告投递在主运行完成后运行，且为尽力而为；<code>status: &quot;ok&quot;</code> 不保证通告已投递。</li><li>通过 Gateway 网关 <code>agent.wait</code>（服务器端）等待，因此重连不会丢失等待。</li><li>智能体到智能体的消息上下文会为主运行注入。</li><li>主运行完成后，OpenSoul 运行<strong>回复循环</strong>： <ul><li>第 2 轮及以后在请求者和目标智能体之间交替。</li><li>精确回复 <code>REPLY_SKIP</code> 以停止来回。</li><li>最大轮数为 <code>session.agentToAgent.maxPingPongTurns</code>（0–5，默认 5）。</li></ul></li><li>循环结束后，OpenSoul 运行<strong>智能体到智能体通告步骤</strong>（仅目标智能体）： <ul><li>精确回复 <code>ANNOUNCE_SKIP</code> 以保持静默。</li><li>任何其他回复都会发送到目标渠道。</li><li>通告步骤包括原始请求 + 第 1 轮回复 + 最新的来回回复。</li></ul></li></ul><h2 id="channel-字段" tabindex="-1"><a class="header-anchor" href="#channel-字段">#</a> Channel 字段</h2><ul><li>对于群组，<code>channel</code> 是会话条目上记录的渠道。</li><li>对于直接聊天，<code>channel</code> 从 <code>lastChannel</code> 映射。</li><li>对于 cron/hook/node，<code>channel</code> 是 <code>internal</code>。</li><li>如果缺失，<code>channel</code> 是 <code>unknown</code>。</li></ul><h2 id="安全-发送策略" tabindex="-1"><a class="header-anchor" href="#安全-发送策略">#</a> 安全 / 发送策略</h2><p>基于策略的按渠道/聊天类型阻止（不是按会话 id）。</p><div class="language-json vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;session&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;sendPolicy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;rules&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          &quot;match&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;channel&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;discord&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;chatType&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;group&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          &quot;action&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;deny&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;default&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;allow&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>运行时覆盖（按会话条目）：</p><ul><li><code>sendPolicy: &quot;allow&quot; | &quot;deny&quot;</code>（未设置 = 继承配置）</li><li>可通过 <code>sessions.patch</code> 或仅所有者的 <code>/send on|off|inherit</code>（独立消息）设置。</li></ul><p>执行点：</p><ul><li><code>chat.send</code> / <code>agent</code>（Gateway 网关）</li><li>自动回复投递逻辑</li></ul><h2 id="sessions-spawn" tabindex="-1"><a class="header-anchor" href="#sessions-spawn">#</a> sessions_spawn</h2><p>在隔离会话中生成子智能体运行，并将结果通告回请求者聊天渠道。</p><p>参数：</p><ul><li><code>task</code>（必填）</li><li><code>label?</code>（可选；用于日志/UI）</li><li><code>agentId?</code>（可选；如果允许，在另一个智能体 id 下生成）</li><li><code>model?</code>（可选；覆盖子智能体模型；无效值会报错）</li><li><code>runTimeoutSeconds?</code>（默认 0；设置时，在 N 秒后中止子智能体运行）</li><li><code>cleanup?</code>（<code>delete|keep</code>，默认 <code>keep</code>）</li></ul><p>允许列表：</p><ul><li><code>agents.list[].subagents.allowAgents</code>：通过 <code>agentId</code> 允许的智能体 id 列表（<code>[&quot;*&quot;]</code> 允许任意）。默认：仅请求者智能体。</li></ul><p>发现：</p><ul><li>使用 <code>agents_list</code> 发现哪些智能体 id 允许用于 <code>sessions_spawn</code>。</li></ul><p>行为：</p><ul><li>使用 <code>deliver: false</code> 启动新的 <code>agent:&lt;agentId&gt;:subagent:&lt;uuid&gt;</code> 会话。</li><li>子智能体默认使用完整工具集<strong>减去会话工具</strong>（可通过 <code>tools.subagents.tools</code> 配置）。</li><li>子智能体不允许调用 <code>sessions_spawn</code>（无子智能体 → 子智能体生成）。</li><li>始终非阻塞：立即返回 <code>{ status: &quot;accepted&quot;, runId, childSessionKey }</code>。</li><li>完成后，OpenSoul 运行子智能体<strong>通告步骤</strong>并将结果发布到请求者聊天渠道。</li><li>在通告步骤中精确回复 <code>ANNOUNCE_SKIP</code> 以保持静默。</li><li>通告回复规范化为 <code>Status</code>/<code>Result</code>/<code>Notes</code>；<code>Status</code> 来自运行时结果（不是模型文本）。</li><li>子智能体会话在 <code>agents.defaults.subagents.archiveAfterMinutes</code> 后自动归档（默认：60）。</li><li>通告回复包含统计行（运行时间、token 数、sessionKey/sessionId、记录路径和可选成本）。</li></ul><h2 id="沙箱会话可见性" tabindex="-1"><a class="header-anchor" href="#沙箱会话可见性">#</a> 沙箱会话可见性</h2><p>沙箱隔离的会话可以使用会话工具，但默认情况下它们只能看到通过 <code>sessions_spawn</code> 生成的会话。</p><p>配置：</p><div class="language-json5 vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json5</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  agents</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    defaults</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 默认：&quot;spawned&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        sessionToolsVisibility</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;spawned&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 或 &quot;all&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div>`,50)])])}const k=e(a,[["render",o]]);export{u as __pageData,k as default};
