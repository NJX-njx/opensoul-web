import{_ as s,o as a,c as n,ai as i}from"./chunks/framework.CsYGeNYI.js";const h=JSON.parse('{"title":"Command Queue","description":"","frontmatter":{"summary":"Command queue design that serializes inbound auto-reply runs","read_when":["Changing auto-reply execution or concurrency"],"title":"Command Queue"},"headers":[],"relativePath":"concepts/queue.md","filePath":"concepts/queue.md","lastUpdated":null}'),l={name:"concepts/queue.md"};function o(t,e,r,d,c,u){return a(),n("div",null,[...e[0]||(e[0]=[i(`<h1 id="command-queue-2026-01-16" tabindex="-1"><a class="header-anchor" href="#command-queue-2026-01-16">#</a> Command Queue (2026-01-16)</h1><p>We serialize inbound auto-reply runs (all channels) through a tiny in-process queue to prevent multiple agent runs from colliding, while still allowing safe parallelism across sessions.</p><h2 id="why" tabindex="-1"><a class="header-anchor" href="#why">#</a> Why</h2><ul><li>Auto-reply runs can be expensive (LLM calls) and can collide when multiple inbound messages arrive close together.</li><li>Serializing avoids competing for shared resources (session files, logs, CLI stdin) and reduces the chance of upstream rate limits.</li></ul><h2 id="how-it-works" tabindex="-1"><a class="header-anchor" href="#how-it-works">#</a> How it works</h2><ul><li>A lane-aware FIFO queue drains each lane with a configurable concurrency cap (default 1 for unconfigured lanes; main defaults to 4, subagent to 8).</li><li><code>runEmbeddedPiAgent</code> enqueues by <strong>session key</strong> (lane <code>session:&lt;key&gt;</code>) to guarantee only one active run per session.</li><li>Each session run is then queued into a <strong>global lane</strong> (<code>main</code> by default) so overall parallelism is capped by <code>agents.defaults.maxConcurrent</code>.</li><li>When verbose logging is enabled, queued runs emit a short notice if they waited more than ~2s before starting.</li><li>Typing indicators still fire immediately on enqueue (when supported by the channel) so user experience is unchanged while we wait our turn.</li></ul><h2 id="queue-modes-per-channel" tabindex="-1"><a class="header-anchor" href="#queue-modes-per-channel">#</a> Queue modes (per channel)</h2><p>Inbound messages can steer the current run, wait for a followup turn, or do both:</p><ul><li><code>steer</code>: inject immediately into the current run (cancels pending tool calls after the next tool boundary). If not streaming, falls back to followup.</li><li><code>followup</code>: enqueue for the next agent turn after the current run ends.</li><li><code>collect</code>: coalesce all queued messages into a <strong>single</strong> followup turn (default). If messages target different channels/threads, they drain individually to preserve routing.</li><li><code>steer-backlog</code> (aka <code>steer+backlog</code>): steer now <strong>and</strong> preserve the message for a followup turn.</li><li><code>interrupt</code> (legacy): abort the active run for that session, then run the newest message.</li><li><code>queue</code> (legacy alias): same as <code>steer</code>.</li></ul><p>Steer-backlog means you can get a followup response after the steered run, so streaming surfaces can look like duplicates. Prefer <code>collect</code>/<code>steer</code> if you want one response per inbound message. Send <code>/queue collect</code> as a standalone command (per-session) or set <code>messages.queue.byChannel.discord: &quot;collect&quot;</code>.</p><p>Defaults (when unset in config):</p><ul><li>All surfaces → <code>collect</code></li></ul><p>Configure globally or per channel via <code>messages.queue</code>:</p><div class="language-json5 vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json5</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  messages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    queue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;collect&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      debounceMs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      cap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      drop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;summarize&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      byChannel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">discord</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;collect&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><h2 id="queue-options" tabindex="-1"><a class="header-anchor" href="#queue-options">#</a> Queue options</h2><p>Options apply to <code>followup</code>, <code>collect</code>, and <code>steer-backlog</code> (and to <code>steer</code> when it falls back to followup):</p><ul><li><code>debounceMs</code>: wait for quiet before starting a followup turn (prevents “continue, continue”).</li><li><code>cap</code>: max queued messages per session.</li><li><code>drop</code>: overflow policy (<code>old</code>, <code>new</code>, <code>summarize</code>).</li></ul><p>Summarize keeps a short bullet list of dropped messages and injects it as a synthetic followup prompt. Defaults: <code>debounceMs: 1000</code>, <code>cap: 20</code>, <code>drop: summarize</code>.</p><h2 id="per-session-overrides" tabindex="-1"><a class="header-anchor" href="#per-session-overrides">#</a> Per-session overrides</h2><ul><li>Send <code>/queue &lt;mode&gt;</code> as a standalone command to store the mode for the current session.</li><li>Options can be combined: <code>/queue collect debounce:2s cap:25 drop:summarize</code></li><li><code>/queue default</code> or <code>/queue reset</code> clears the session override.</li></ul><h2 id="scope-and-guarantees" tabindex="-1"><a class="header-anchor" href="#scope-and-guarantees">#</a> Scope and guarantees</h2><ul><li>Applies to auto-reply agent runs across all inbound channels that use the gateway reply pipeline (WhatsApp web, Telegram, Slack, Discord, Signal, iMessage, webchat, etc.).</li><li>Default lane (<code>main</code>) is process-wide for inbound + main heartbeats; set <code>agents.defaults.maxConcurrent</code> to allow multiple sessions in parallel.</li><li>Additional lanes may exist (e.g. <code>cron</code>, <code>subagent</code>) so background jobs can run in parallel without blocking inbound replies.</li><li>Per-session lanes guarantee that only one agent run touches a given session at a time.</li><li>No external dependencies or background worker threads; pure TypeScript + promises.</li></ul><h2 id="troubleshooting" tabindex="-1"><a class="header-anchor" href="#troubleshooting">#</a> Troubleshooting</h2><ul><li>If commands seem stuck, enable verbose logs and look for “queued for …ms” lines to confirm the queue is draining.</li><li>If you need queue depth, enable verbose logs and watch for queue timing lines.</li></ul>`,24)])])}const g=s(l,[["render",o]]);export{h as __pageData,g as default};
