import{_ as s,o as a,c as i,ai as n}from"./chunks/framework.CsYGeNYI.js";const g=JSON.parse('{"title":"Development Channels","description":"","frontmatter":{"summary":"Stable, beta, and dev channels: semantics, switching, and tagging","read_when":["You want to switch between stable/beta/dev","You are tagging or publishing prereleases"],"title":"Development Channels"},"headers":[],"relativePath":"install/development-channels.md","filePath":"install/development-channels.md","lastUpdated":1771906665000}'),t={name:"install/development-channels.md"};function l(d,e,p,o,h,c){return a(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="development-channels" tabindex="-1"><a class="header-anchor" href="#development-channels">#</a> Development channels</h1><p>Last updated: 2026-01-21</p><p>OpenSoul ships three update channels:</p><ul><li><strong>stable</strong>: npm dist-tag <code>latest</code>.</li><li><strong>beta</strong>: npm dist-tag <code>beta</code> (builds under test).</li><li><strong>dev</strong>: moving head of <code>main</code> (git). npm dist-tag: <code>dev</code> (when published).</li></ul><p>We ship builds to <strong>beta</strong>, test them, then <strong>promote a vetted build to <code>latest</code></strong> without changing the version number — dist-tags are the source of truth for npm installs.</p><h2 id="switching-channels" tabindex="-1"><a class="header-anchor" href="#switching-channels">#</a> Switching channels</h2><p>Git checkout:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --channel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stable</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --channel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> beta</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --channel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><ul><li><code>stable</code>/<code>beta</code> check out the latest matching tag (often the same tag).</li><li><code>dev</code> switches to <code>main</code> and rebases on the upstream.</li></ul><p>npm/pnpm global install:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --channel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stable</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --channel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> beta</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --channel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>This updates via the corresponding npm dist-tag (<code>latest</code>, <code>beta</code>, <code>dev</code>).</p><p>When you <strong>explicitly</strong> switch channels with <code>--channel</code>, OpenSoul also aligns the install method:</p><ul><li><code>dev</code> ensures a git checkout (default <code>~/opensoul</code>, override with <code>OPENSOUL_GIT_DIR</code>), updates it, and installs the global CLI from that checkout.</li><li><code>stable</code>/<code>beta</code> installs from npm using the matching dist-tag.</li></ul><p>Tip: if you want stable + dev in parallel, keep two clones and point your gateway at the stable one.</p><h2 id="plugins-and-channels" tabindex="-1"><a class="header-anchor" href="#plugins-and-channels">#</a> Plugins and channels</h2><p>When you switch channels with <code>opensoul update</code>, OpenSoul also syncs plugin sources:</p><ul><li><code>dev</code> prefers bundled plugins from the git checkout.</li><li><code>stable</code> and <code>beta</code> restore npm-installed plugin packages.</li></ul><h2 id="tagging-best-practices" tabindex="-1"><a class="header-anchor" href="#tagging-best-practices">#</a> Tagging best practices</h2><ul><li>Tag releases you want git checkouts to land on (<code>vYYYY.M.D</code> or <code>vYYYY.M.D-&lt;patch&gt;</code>).</li><li>Keep tags immutable: never move or reuse a tag.</li><li>npm dist-tags remain the source of truth for npm installs: <ul><li><code>latest</code> → stable</li><li><code>beta</code> → candidate build</li><li><code>dev</code> → main snapshot (optional)</li></ul></li></ul><h2 id="macos-app-availability" tabindex="-1"><a class="header-anchor" href="#macos-app-availability">#</a> macOS app availability</h2><p>Beta and dev builds may <strong>not</strong> include a macOS app release. That’s OK:</p><ul><li>The git tag and npm dist-tag can still be published.</li><li>Call out “no macOS build for this beta” in release notes or changelog.</li></ul>`,23)])])}const u=s(t,[["render",l]]);export{g as __pageData,u as default};
