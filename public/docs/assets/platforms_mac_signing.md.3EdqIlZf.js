import{_ as s,o as a,c as i,ai as n}from"./chunks/framework.CsYGeNYI.js";const g=JSON.parse('{"title":"macOS Signing","description":"","frontmatter":{"summary":"Signing steps for macOS debug builds generated by packaging scripts","read_when":["Building or signing mac debug builds"],"title":"macOS Signing"},"headers":[],"relativePath":"platforms/mac/signing.md","filePath":"platforms/mac/signing.md","lastUpdated":null}'),t={name:"platforms/mac/signing.md"};function o(r,e,p,l,d,h){return a(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="mac-signing-debug-builds" tabindex="-1"><a class="header-anchor" href="#mac-signing-debug-builds">#</a> mac signing (debug builds)</h1><p>This app is usually built from <a href="https://github.com/NJX-njx/opensoul/blob/main/scripts/package-mac-app.sh" target="_blank" rel="noreferrer"><code>scripts/package-mac-app.sh</code></a>, which now:</p><ul><li>sets a stable debug bundle identifier: <code>ai.opensoul.mac.debug</code></li><li>writes the Info.plist with that bundle id (override via <code>BUNDLE_ID=...</code>)</li><li>calls <a href="https://github.com/NJX-njx/opensoul/blob/main/scripts/codesign-mac-app.sh" target="_blank" rel="noreferrer"><code>scripts/codesign-mac-app.sh</code></a> to sign the main binary and app bundle so macOS treats each rebuild as the same signed bundle and keeps TCC permissions (notifications, accessibility, screen recording, mic, speech). For stable permissions, use a real signing identity; ad-hoc is opt-in and fragile (see <a href="/docs/platforms/mac/permissions">macOS permissions</a>).</li><li>uses <code>CODESIGN_TIMESTAMP=auto</code> by default; it enables trusted timestamps for Developer ID signatures. Set <code>CODESIGN_TIMESTAMP=off</code> to skip timestamping (offline debug builds).</li><li>inject build metadata into Info.plist: <code>OpenSoulBuildTimestamp</code> (UTC) and <code>OpenSoulGitCommit</code> (short hash) so the About pane can show build, git, and debug/release channel.</li><li><strong>Packaging requires Node 22+</strong>: the script runs TS builds and the Control UI build.</li><li>reads <code>SIGN_IDENTITY</code> from the environment. Add <code>export SIGN_IDENTITY=&quot;Apple Development: Your Name (TEAMID)&quot;</code> (or your Developer ID Application cert) to your shell rc to always sign with your cert. Ad-hoc signing requires explicit opt-in via <code>ALLOW_ADHOC_SIGNING=1</code> or <code>SIGN_IDENTITY=&quot;-&quot;</code> (not recommended for permission testing).</li><li>runs a Team ID audit after signing and fails if any Mach-O inside the app bundle is signed by a different Team ID. Set <code>SKIP_TEAM_ID_CHECK=1</code> to bypass.</li></ul><h2 id="usage" tabindex="-1"><a class="header-anchor" href="#usage">#</a> Usage</h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># from repo root</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/package-mac-app.sh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               # auto-selects identity; errors if none found</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SIGN_IDENTITY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Developer ID Application: Your Name&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> scripts/package-mac-app.sh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # real cert</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ALLOW_ADHOC_SIGNING</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">1</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> scripts/package-mac-app.sh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # ad-hoc (permissions will not stick)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SIGN_IDENTITY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;-&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> scripts/package-mac-app.sh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # explicit ad-hoc (same caveat)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">DISABLE_LIBRARY_VALIDATION</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">1</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> scripts/package-mac-app.sh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # dev-only Sparkle Team ID mismatch workaround</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><h3 id="ad-hoc-signing-note" tabindex="-1"><a class="header-anchor" href="#ad-hoc-signing-note">#</a> Ad-hoc Signing Note</h3><p>When signing with <code>SIGN_IDENTITY=&quot;-&quot;</code> (ad-hoc), the script automatically disables the <strong>Hardened Runtime</strong> (<code>--options runtime</code>). This is necessary to prevent crashes when the app attempts to load embedded frameworks (like Sparkle) that do not share the same Team ID. Ad-hoc signatures also break TCC permission persistence; see <a href="/docs/platforms/mac/permissions">macOS permissions</a> for recovery steps.</p><h2 id="build-metadata-for-about" tabindex="-1"><a class="header-anchor" href="#build-metadata-for-about">#</a> Build metadata for About</h2><p><code>package-mac-app.sh</code> stamps the bundle with:</p><ul><li><code>OpenSoulBuildTimestamp</code>: ISO8601 UTC at package time</li><li><code>OpenSoulGitCommit</code>: short git hash (or <code>unknown</code> if unavailable)</li></ul><p>The About tab reads these keys to show version, build date, git commit, and whether it’s a debug build (via <code>#if DEBUG</code>). Run the packager to refresh these values after code changes.</p><h2 id="why" tabindex="-1"><a class="header-anchor" href="#why">#</a> Why</h2><p>TCC permissions are tied to the bundle identifier <em>and</em> code signature. Unsigned debug builds with changing UUIDs were causing macOS to forget grants after each rebuild. Signing the binaries (ad‑hoc by default) and keeping a fixed bundle id/path (<code>dist/OpenSoul.app</code>) preserves the grants between builds, matching the VibeTunnel approach.</p>`,13)])])}const u=s(t,[["render",o]]);export{g as __pageData,u as default};
