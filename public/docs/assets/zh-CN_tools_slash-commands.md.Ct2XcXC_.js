import{_ as s,o,c as a,ai as l}from"./chunks/framework.CsYGeNYI.js";const u=JSON.parse('{"title":"斜杠命令","description":"","frontmatter":{"read_when":["使用或配置聊天命令","调试命令路由或权限"],"summary":"斜杠命令：文本 vs 原生、配置和支持的命令","title":"斜杠命令","x-i18n":{"generated_at":"2026-02-03T10:12:40Z","model":"claude-opus-4-5","provider":"pi","source_hash":"ca0deebf89518e8c62828fbb9bf4621c5fff8ab86ccb22e37da61a28f9a7886a","source_path":"tools/slash-commands.md","workflow":15}},"headers":[],"relativePath":"zh-CN/tools/slash-commands.md","filePath":"zh-CN/tools/slash-commands.md","lastUpdated":null}'),i={name:"zh-CN/tools/slash-commands.md"};function n(d,e,c,t,p,r){return o(),a("div",null,[...e[0]||(e[0]=[l(`<h1 id="斜杠命令" tabindex="-1"><a class="header-anchor" href="#斜杠命令">#</a> 斜杠命令</h1><p>命令由 Gateway 网关处理。大多数命令必须作为以 <code>/</code> 开头的<strong>独立</strong>消息发送。 仅主机的 bash 聊天命令使用 <code>! &lt;cmd&gt;</code>（<code>/bash &lt;cmd&gt;</code> 是别名）。</p><p>有两个相关系统：</p><ul><li><strong>命令</strong>：独立的 <code>/...</code> 消息。</li><li><strong>指令</strong>：<code>/think</code>、<code>/verbose</code>、<code>/reasoning</code>、<code>/elevated</code>、<code>/exec</code>、<code>/model</code>、<code>/queue</code>。 <ul><li>指令在模型看到消息之前被剥离。</li><li>在普通聊天消息中（不是仅指令消息），它们被视为&quot;内联提示&quot;，<strong>不会</strong>持久化会话设置。</li><li>在仅指令消息中（消息只包含指令），它们会持久化到会话并回复确认。</li><li>指令仅对<strong>授权发送者</strong>生效（渠道白名单/配对加上 <code>commands.useAccessGroups</code>）。 未授权发送者的指令被视为纯文本。</li></ul></li></ul><p>还有一些<strong>内联快捷方式</strong>（仅限白名单/授权发送者）：<code>/help</code>、<code>/commands</code>、<code>/status</code>、<code>/whoami</code>（<code>/id</code>）。 它们立即运行，在模型看到消息之前被剥离，剩余文本继续通过正常流程。</p><h2 id="配置" tabindex="-1"><a class="header-anchor" href="#配置">#</a> 配置</h2><div class="language-json5 vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json5</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  commands</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    native</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;auto&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    nativeSkills</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;auto&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    bash</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    bashForegroundMs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    debug</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    restart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    useAccessGroups</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><ul><li><code>commands.text</code>（默认 <code>true</code>）启用解析聊天消息中的 <code>/...</code>。 <ul><li>在没有原生命令的平台上（WhatsApp/WebChat/Signal/iMessage/Google Chat/MS Teams），即使你将此设置为 <code>false</code>，文本命令仍然有效。</li></ul></li><li><code>commands.native</code>（默认 <code>&quot;auto&quot;</code>）注册原生命令。 <ul><li>Auto：在 Discord/Telegram 上启用；在 Slack 上禁用（直到你添加斜杠命令）；在不支持原生命令的提供商上忽略。</li><li>设置 <code>channels.discord.commands.native</code>、<code>channels.telegram.commands.native</code> 或 <code>channels.slack.commands.native</code> 以按提供商覆盖（布尔值或 <code>&quot;auto&quot;</code>）。</li><li><code>false</code> 在启动时清除 Discord/Telegram 上之前注册的命令。Slack 命令在 Slack 应用中管理，不会自动删除。</li></ul></li><li><code>commands.nativeSkills</code>（默认 <code>&quot;auto&quot;</code>）在支持时原生注册 <strong>Skill</strong> 命令。 <ul><li>Auto：在 Discord/Telegram 上启用；在 Slack 上禁用（Slack 需要为每个 Skill 创建一个斜杠命令）。</li><li>设置 <code>channels.discord.commands.nativeSkills</code>、<code>channels.telegram.commands.nativeSkills</code> 或 <code>channels.slack.commands.nativeSkills</code> 以按提供商覆盖（布尔值或 <code>&quot;auto&quot;</code>）。</li></ul></li><li><code>commands.bash</code>（默认 <code>false</code>）启用 <code>! &lt;cmd&gt;</code> 来运行主机 shell 命令（<code>/bash &lt;cmd&gt;</code> 是别名；需要 <code>tools.elevated</code> 白名单）。</li><li><code>commands.bashForegroundMs</code>（默认 <code>2000</code>）控制 bash 切换到后台模式之前等待多长时间（<code>0</code> 立即后台运行）。</li><li><code>commands.config</code>（默认 <code>false</code>）启用 <code>/config</code>（读写 <code>opensoul.json</code>）。</li><li><code>commands.debug</code>（默认 <code>false</code>）启用 <code>/debug</code>（仅运行时覆盖）。</li><li><code>commands.useAccessGroups</code>（默认 <code>true</code>）对命令强制执行白名单/策略。</li></ul><h2 id="命令列表" tabindex="-1"><a class="header-anchor" href="#命令列表">#</a> 命令列表</h2><p>文本 + 原生（启用时）：</p><ul><li><code>/help</code></li><li><code>/commands</code></li><li><code>/skill &lt;name&gt; [input]</code>（按名称运行 Skill）</li><li><code>/status</code>（显示当前状态；在可用时包含当前模型提供商的提供商使用量/配额）</li><li><code>/allowlist</code>（列出/添加/删除白名单条目）</li><li><code>/approve &lt;id&gt; allow-once|allow-always|deny</code>（解决 exec 审批提示）</li><li><code>/context [list|detail|json]</code>（解释&quot;上下文&quot;；<code>detail</code> 显示每个文件 + 每个工具 + 每个 Skill + 系统提示词大小）</li><li><code>/whoami</code>（显示你的发送者 ID；别名：<code>/id</code>）</li><li><code>/subagents list|stop|log|info|send</code>（检查、停止、记录或向当前会话的子智能体运行发送消息）</li><li><code>/config show|get|set|unset</code>（将配置持久化到磁盘，仅所有者；需要 <code>commands.config: true</code>）</li><li><code>/debug show|set|unset|reset</code>（运行时覆盖，仅所有者；需要 <code>commands.debug: true</code>）</li><li><code>/usage off|tokens|full|cost</code>（每响应使用量页脚或本地成本摘要）</li><li><code>/tts off|always|inbound|tagged|status|provider|limit|summary|audio</code>（控制 TTS；参见 <a href="/docs/tts">/tts</a>） <ul><li>Discord：原生命令是 <code>/voice</code>（Discord 保留了 <code>/tts</code>）；文本 <code>/tts</code> 仍然有效。</li></ul></li><li><code>/stop</code></li><li><code>/restart</code></li><li><code>/dock-telegram</code>（别名：<code>/dock_telegram</code>）（将回复切换到 Telegram）</li><li><code>/dock-discord</code>（别名：<code>/dock_discord</code>）（将回复切换到 Discord）</li><li><code>/dock-slack</code>（别名：<code>/dock_slack</code>）（将回复切换到 Slack）</li><li><code>/activation mention|always</code>（仅限群组）</li><li><code>/send on|off|inherit</code>（仅所有者）</li><li><code>/reset</code> 或 <code>/new [model]</code>（可选模型提示；其余部分传递）</li><li><code>/think &lt;off|minimal|low|medium|high|xhigh&gt;</code>（按模型/提供商动态选择；别名：<code>/thinking</code>、<code>/t</code>）</li><li><code>/verbose on|full|off</code>（别名：<code>/v</code>）</li><li><code>/reasoning on|off|stream</code>（别名：<code>/reason</code>；启用时，发送带有 <code>Reasoning:</code> 前缀的单独消息；<code>stream</code> = 仅 Telegram 草稿）</li><li><code>/elevated on|off|ask|full</code>（别名：<code>/elev</code>；<code>full</code> 跳过 exec 审批）</li><li><code>/exec host=&lt;sandbox|gateway|node&gt; security=&lt;deny|allowlist|full&gt; ask=&lt;off|on-miss|always&gt; node=&lt;id&gt;</code>（发送 <code>/exec</code> 显示当前设置）</li><li><code>/model &lt;name&gt;</code>（别名：<code>/models</code>；或 <code>agents.defaults.models.*.alias</code> 中的 <code>/&lt;alias&gt;</code>）</li><li><code>/queue &lt;mode&gt;</code>（加上选项如 <code>debounce:2s cap:25 drop:summarize</code>；发送 <code>/queue</code> 查看当前设置）</li><li><code>/bash &lt;command&gt;</code>（仅主机；<code>! &lt;command&gt;</code> 的别名；需要 <code>commands.bash: true</code> + <code>tools.elevated</code> 白名单）</li></ul><p>仅文本：</p><ul><li><code>/compact [instructions]</code>（参见 <a href="/docs/concepts/compaction">/concepts/compaction</a>）</li><li><code>! &lt;command&gt;</code>（仅主机；一次一个；对长时间运行的任务使用 <code>!poll</code> + <code>!stop</code>）</li><li><code>!poll</code>（检查输出/状态；接受可选的 <code>sessionId</code>；<code>/bash poll</code> 也可用）</li><li><code>!stop</code>（停止正在运行的 bash 任务；接受可选的 <code>sessionId</code>；<code>/bash stop</code> 也可用）</li></ul><p>注意事项：</p><ul><li>命令接受命令和参数之间的可选 <code>:</code>（例如 <code>/think: high</code>、<code>/send: on</code>、<code>/help:</code>）。</li><li><code>/new &lt;model&gt;</code> 接受模型别名、<code>provider/model</code> 或提供商名称（模糊匹配）；如果没有匹配，文本被视为消息正文。</li><li>要获取完整的提供商使用量分解，使用 <code>opensoul status --usage</code>。</li><li><code>/allowlist add|remove</code> 需要 <code>commands.config=true</code> 并遵循渠道 <code>configWrites</code>。</li><li><code>/usage</code> 控制每响应使用量页脚；<code>/usage cost</code> 从 OpenSoul 会话日志打印本地成本摘要。</li><li><code>/restart</code> 默认禁用；设置 <code>commands.restart: true</code> 启用它。</li><li><code>/verbose</code> 用于调试和额外可见性；在正常使用中保持<strong>关闭</strong>。</li><li><code>/reasoning</code>（和 <code>/verbose</code>）在群组设置中有风险：它们可能会暴露你不打算公开的内部推理或工具输出。最好保持关闭，尤其是在群聊中。</li><li><strong>快速路径：</strong> 来自白名单发送者的仅命令消息会立即处理（绕过队列 + 模型）。</li><li><strong>群组提及门控：</strong> 来自白名单发送者的仅命令消息绕过提及要求。</li><li><strong>内联快捷方式（仅限白名单发送者）：</strong> 某些命令在嵌入普通消息时也能工作，并在模型看到剩余文本之前被剥离。 <ul><li>示例：<code>hey /status</code> 触发状态回复，剩余文本继续通过正常流程。</li></ul></li><li>目前：<code>/help</code>、<code>/commands</code>、<code>/status</code>、<code>/whoami</code>（<code>/id</code>）。</li><li>未授权的仅命令消息被静默忽略，内联 <code>/...</code> 令牌被视为纯文本。</li><li><strong>Skill 命令：</strong> <code>user-invocable</code> Skills 作为斜杠命令公开。名称被清理为 <code>a-z0-9_</code>（最多 32 个字符）；冲突获得数字后缀（例如 <code>_2</code>）。 <ul><li><code>/skill &lt;name&gt; [input]</code> 按名称运行 Skill（当原生命令限制阻止每个 Skill 命令时有用）。</li><li>默认情况下，Skill 命令作为普通请求转发给模型。</li><li>Skills 可以选择声明 <code>command-dispatch: tool</code> 将命令直接路由到工具（确定性，无模型）。</li><li>示例：<code>/prose</code>（OpenProse 插件）— 参见 <a href="/docs/prose">OpenProse</a>。</li></ul></li><li><strong>原生命令参数：</strong> Discord 使用自动完成进行动态选项（以及当你省略必需参数时的按钮菜单）。当命令支持选择且你省略参数时，Telegram 和 Slack 显示按钮菜单。</li></ul><h2 id="使用量显示-什么显示在哪里" tabindex="-1"><a class="header-anchor" href="#使用量显示-什么显示在哪里">#</a> 使用量显示（什么显示在哪里）</h2><ul><li><strong>提供商使用量/配额</strong>（示例：&quot;Claude 80% left&quot;）在启用使用量跟踪时显示在 <code>/status</code> 中，针对当前模型提供商。</li><li><strong>每响应令牌/成本</strong>由 <code>/usage off|tokens|full</code> 控制（附加到普通回复）。</li><li><code>/model status</code> 是关于<strong>模型/认证/端点</strong>的，不是使用量。</li></ul><h2 id="模型选择-model" tabindex="-1"><a class="header-anchor" href="#模型选择-model">#</a> 模型选择（<code>/model</code>）</h2><p><code>/model</code> 作为指令实现。</p><p>示例：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/model</span></span>
<span class="line"><span>/model list</span></span>
<span class="line"><span>/model 3</span></span>
<span class="line"><span>/model openai/gpt-5.2</span></span>
<span class="line"><span>/model opus@anthropic:default</span></span>
<span class="line"><span>/model status</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>注意事项：</p><ul><li><code>/model</code> 和 <code>/model list</code> 显示紧凑的编号选择器（模型系列 + 可用提供商）。</li><li><code>/model &lt;#&gt;</code> 从该选择器中选择（并在可能时优先选择当前提供商）。</li><li><code>/model status</code> 显示详细视图，包括在可用时配置的提供商端点（<code>baseUrl</code>）和 API 模式（<code>api</code>）。</li></ul><h2 id="调试覆盖" tabindex="-1"><a class="header-anchor" href="#调试覆盖">#</a> 调试覆盖</h2><p><code>/debug</code> 让你设置<strong>仅运行时</strong>的配置覆盖（内存，不写磁盘）。仅所有者。默认禁用；使用 <code>commands.debug: true</code> 启用。</p><p>示例：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/debug show</span></span>
<span class="line"><span>/debug set messages.responsePrefix=&quot;[opensoul]&quot;</span></span>
<span class="line"><span>/debug set channels.whatsapp.allowFrom=[&quot;+1555&quot;,&quot;+4477&quot;]</span></span>
<span class="line"><span>/debug unset messages.responsePrefix</span></span>
<span class="line"><span>/debug reset</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>注意事项：</p><ul><li>覆盖立即应用于新的配置读取，但<strong>不会</strong>写入 <code>opensoul.json</code>。</li><li>使用 <code>/debug reset</code> 清除所有覆盖并返回到磁盘上的配置。</li></ul><h2 id="配置更新" tabindex="-1"><a class="header-anchor" href="#配置更新">#</a> 配置更新</h2><p><code>/config</code> 写入你的磁盘配置（<code>opensoul.json</code>）。仅所有者。默认禁用；使用 <code>commands.config: true</code> 启用。</p><p>示例：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/config show</span></span>
<span class="line"><span>/config show messages.responsePrefix</span></span>
<span class="line"><span>/config get messages.responsePrefix</span></span>
<span class="line"><span>/config set messages.responsePrefix=&quot;[opensoul]&quot;</span></span>
<span class="line"><span>/config unset messages.responsePrefix</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>注意事项：</p><ul><li>配置在写入前会验证；无效更改会被拒绝。</li><li><code>/config</code> 更新在重启后持久化。</li></ul><h2 id="平台注意事项" tabindex="-1"><a class="header-anchor" href="#平台注意事项">#</a> 平台注意事项</h2><ul><li><strong>文本命令</strong>在普通聊天会话中运行（私信共享 <code>main</code>，群组有自己的会话）。</li><li><strong>原生命令</strong>使用隔离的会话： <ul><li>Discord：<code>agent:&lt;agentId&gt;:discord:slash:&lt;userId&gt;</code></li><li>Slack：<code>agent:&lt;agentId&gt;:slack:slash:&lt;userId&gt;</code>（前缀可通过 <code>channels.slack.slashCommand.sessionPrefix</code> 配置）</li><li>Telegram：<code>telegram:slash:&lt;userId&gt;</code>（通过 <code>CommandTargetSessionKey</code> 定向到聊天会话）</li></ul></li><li><strong><code>/stop</code></strong> 定向到活动聊天会话，因此可以中止当前运行。</li><li><strong>Slack：</strong> <code>channels.slack.slashCommand</code> 仍然支持单个 <code>/opensoul</code> 风格的命令。如果你启用 <code>commands.native</code>，你必须为每个内置命令创建一个 Slack 斜杠命令（与 <code>/help</code> 相同的名称）。Slack 的命令参数菜单以临时 Block Kit 按钮形式发送。</li></ul>`,37)])])}const g=s(i,[["render",n]]);export{u as __pageData,g as default};
