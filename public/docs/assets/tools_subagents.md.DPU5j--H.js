import{_ as s,o as n,c as a,ai as i}from"./chunks/framework.CsYGeNYI.js";const h=JSON.parse('{"title":"Sub-Agents","description":"","frontmatter":{"summary":"Sub-agents: spawning isolated agent runs that announce results back to the requester chat","read_when":["You want background/parallel work via the agent","You are changing sessions_spawn or sub-agent tool policy"],"title":"Sub-Agents"},"headers":[],"relativePath":"tools/subagents.md","filePath":"tools/subagents.md","lastUpdated":1771906665000}'),t={name:"tools/subagents.md"};function o(l,e,d,r,c,p){return n(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="sub-agents" tabindex="-1"><a class="header-anchor" href="#sub-agents">#</a> Sub-agents</h1><p>Sub-agents are background agent runs spawned from an existing agent run. They run in their own session (<code>agent:&lt;agentId&gt;:subagent:&lt;uuid&gt;</code>) and, when finished, <strong>announce</strong> their result back to the requester chat channel.</p><h2 id="slash-command" tabindex="-1"><a class="header-anchor" href="#slash-command">#</a> Slash command</h2><p>Use <code>/subagents</code> to inspect or control sub-agent runs for the <strong>current session</strong>:</p><ul><li><code>/subagents list</code></li><li><code>/subagents stop &lt;id|#|all&gt;</code></li><li><code>/subagents log &lt;id|#&gt; [limit] [tools]</code></li><li><code>/subagents info &lt;id|#&gt;</code></li><li><code>/subagents send &lt;id|#&gt; &lt;message&gt;</code></li></ul><p><code>/subagents info</code> shows run metadata (status, timestamps, session id, transcript path, cleanup).</p><p>Primary goals:</p><ul><li>Parallelize “research / long task / slow tool” work without blocking the main run.</li><li>Keep sub-agents isolated by default (session separation + optional sandboxing).</li><li>Keep the tool surface hard to misuse: sub-agents do <strong>not</strong> get session tools by default.</li><li>Avoid nested fan-out: sub-agents cannot spawn sub-agents.</li></ul><p>Cost note: each sub-agent has its <strong>own</strong> context and token usage. For heavy or repetitive tasks, set a cheaper model for sub-agents and keep your main agent on a higher-quality model. You can configure this via <code>agents.defaults.subagents.model</code> or per-agent overrides.</p><h2 id="tool" tabindex="-1"><a class="header-anchor" href="#tool">#</a> Tool</h2><p>Use <code>sessions_spawn</code>:</p><ul><li>Starts a sub-agent run (<code>deliver: false</code>, global lane: <code>subagent</code>)</li><li>Then runs an announce step and posts the announce reply to the requester chat channel</li><li>Default model: inherits the caller unless you set <code>agents.defaults.subagents.model</code> (or per-agent <code>agents.list[].subagents.model</code>); an explicit <code>sessions_spawn.model</code> still wins.</li><li>Default thinking: inherits the caller unless you set <code>agents.defaults.subagents.thinking</code> (or per-agent <code>agents.list[].subagents.thinking</code>); an explicit <code>sessions_spawn.thinking</code> still wins.</li></ul><p>Tool params:</p><ul><li><code>task</code> (required)</li><li><code>label?</code> (optional)</li><li><code>agentId?</code> (optional; spawn under another agent id if allowed)</li><li><code>model?</code> (optional; overrides the sub-agent model; invalid values are skipped and the sub-agent runs on the default model with a warning in the tool result)</li><li><code>thinking?</code> (optional; overrides thinking level for the sub-agent run)</li><li><code>runTimeoutSeconds?</code> (default <code>0</code>; when set, the sub-agent run is aborted after N seconds)</li><li><code>cleanup?</code> (<code>delete|keep</code>, default <code>keep</code>)</li></ul><p>Allowlist:</p><ul><li><code>agents.list[].subagents.allowAgents</code>: list of agent ids that can be targeted via <code>agentId</code> (<code>[&quot;*&quot;]</code> to allow any). Default: only the requester agent.</li></ul><p>Discovery:</p><ul><li>Use <code>agents_list</code> to see which agent ids are currently allowed for <code>sessions_spawn</code>.</li></ul><p>Auto-archive:</p><ul><li>Sub-agent sessions are automatically archived after <code>agents.defaults.subagents.archiveAfterMinutes</code> (default: 60).</li><li>Archive uses <code>sessions.delete</code> and renames the transcript to <code>*.deleted.&lt;timestamp&gt;</code> (same folder).</li><li><code>cleanup: &quot;delete&quot;</code> archives immediately after announce (still keeps the transcript via rename).</li><li>Auto-archive is best-effort; pending timers are lost if the gateway restarts.</li><li><code>runTimeoutSeconds</code> does <strong>not</strong> auto-archive; it only stops the run. The session remains until auto-archive.</li></ul><h2 id="authentication" tabindex="-1"><a class="header-anchor" href="#authentication">#</a> Authentication</h2><p>Sub-agent auth is resolved by <strong>agent id</strong>, not by session type:</p><ul><li>The sub-agent session key is <code>agent:&lt;agentId&gt;:subagent:&lt;uuid&gt;</code>.</li><li>The auth store is loaded from that agent’s <code>agentDir</code>.</li><li>The main agent’s auth profiles are merged in as a <strong>fallback</strong>; agent profiles override main profiles on conflicts.</li></ul><p>Note: the merge is additive, so main profiles are always available as fallbacks. Fully isolated auth per agent is not supported yet.</p><h2 id="announce" tabindex="-1"><a class="header-anchor" href="#announce">#</a> Announce</h2><p>Sub-agents report back via an announce step:</p><ul><li>The announce step runs inside the sub-agent session (not the requester session).</li><li>If the sub-agent replies exactly <code>ANNOUNCE_SKIP</code>, nothing is posted.</li><li>Otherwise the announce reply is posted to the requester chat channel via a follow-up <code>agent</code> call (<code>deliver=true</code>).</li><li>Announce replies preserve thread/topic routing when available (Slack threads, Telegram topics, Matrix threads).</li><li>Announce messages are normalized to a stable template: <ul><li><code>Status:</code> derived from the run outcome (<code>success</code>, <code>error</code>, <code>timeout</code>, or <code>unknown</code>).</li><li><code>Result:</code> the summary content from the announce step (or <code>(not available)</code> if missing).</li><li><code>Notes:</code> error details and other useful context.</li></ul></li><li><code>Status</code> is not inferred from model output; it comes from runtime outcome signals.</li></ul><p>Announce payloads include a stats line at the end (even when wrapped):</p><ul><li>Runtime (e.g., <code>runtime 5m12s</code>)</li><li>Token usage (input/output/total)</li><li>Estimated cost when model pricing is configured (<code>models.providers.*.models[].cost</code>)</li><li><code>sessionKey</code>, <code>sessionId</code>, and transcript path (so the main agent can fetch history via <code>sessions_history</code> or inspect the file on disk)</li></ul><h2 id="tool-policy-sub-agent-tools" tabindex="-1"><a class="header-anchor" href="#tool-policy-sub-agent-tools">#</a> Tool Policy (sub-agent tools)</h2><p>By default, sub-agents get <strong>all tools except session tools</strong>:</p><ul><li><code>sessions_list</code></li><li><code>sessions_history</code></li><li><code>sessions_send</code></li><li><code>sessions_spawn</code></li></ul><p>Override via config:</p><div class="language-json5 vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json5</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  agents</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    defaults</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      subagents</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        maxConcurrent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  tools</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    subagents</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      tools</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // deny wins</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        deny</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cron&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // if allow is set, it becomes allow-only (deny still wins)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // allow: [&quot;read&quot;, &quot;exec&quot;, &quot;process&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><h2 id="concurrency" tabindex="-1"><a class="header-anchor" href="#concurrency">#</a> Concurrency</h2><p>Sub-agents use a dedicated in-process queue lane:</p><ul><li>Lane name: <code>subagent</code></li><li>Concurrency: <code>agents.defaults.subagents.maxConcurrent</code> (default <code>8</code>)</li></ul><h2 id="stopping" tabindex="-1"><a class="header-anchor" href="#stopping">#</a> Stopping</h2><ul><li>Sending <code>/stop</code> in the requester chat aborts the requester session and stops any active sub-agent runs spawned from it.</li></ul><h2 id="limitations" tabindex="-1"><a class="header-anchor" href="#limitations">#</a> Limitations</h2><ul><li>Sub-agent announce is <strong>best-effort</strong>. If the gateway restarts, pending “announce back” work is lost.</li><li>Sub-agents still share the same gateway process resources; treat <code>maxConcurrent</code> as a safety valve.</li><li><code>sessions_spawn</code> is always non-blocking: it returns <code>{ status: &quot;accepted&quot;, runId, childSessionKey }</code> immediately.</li><li>Sub-agent context only injects <code>AGENTS.md</code> + <code>TOOLS.md</code> (no <code>SOUL.md</code>, <code>IDENTITY.md</code>, <code>USER.md</code>, <code>HEARTBEAT.md</code>, or <code>BOOTSTRAP.md</code>).</li></ul>`,41)])])}const g=s(t,[["render",o]]);export{h as __pageData,g as default};
