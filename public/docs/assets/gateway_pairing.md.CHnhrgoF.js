import{_ as s,o as a,c as i,ai as n}from"./chunks/framework.CsYGeNYI.js";const g=JSON.parse('{"title":"Gateway-Owned Pairing","description":"","frontmatter":{"summary":"Gateway-owned node pairing (Option B) for iOS and other remote nodes","read_when":["Implementing node pairing approvals without macOS UI","Adding CLI flows for approving remote nodes","Extending gateway protocol with node management"],"title":"Gateway-Owned Pairing"},"headers":[],"relativePath":"gateway/pairing.md","filePath":"gateway/pairing.md","lastUpdated":null}'),t={name:"gateway/pairing.md"};function o(r,e,p,l,d,h){return a(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="gateway-owned-pairing-option-b" tabindex="-1"><a class="header-anchor" href="#gateway-owned-pairing-option-b">#</a> Gateway-owned pairing (Option B)</h1><p>In Gateway-owned pairing, the <strong>Gateway</strong> is the source of truth for which nodes are allowed to join. UIs (macOS app, future clients) are just frontends that approve or reject pending requests.</p><p><strong>Important:</strong> WS nodes use <strong>device pairing</strong> (role <code>node</code>) during <code>connect</code>. <code>node.pair.*</code> is a separate pairing store and does <strong>not</strong> gate the WS handshake. Only clients that explicitly call <code>node.pair.*</code> use this flow.</p><h2 id="concepts" tabindex="-1"><a class="header-anchor" href="#concepts">#</a> Concepts</h2><ul><li><strong>Pending request</strong>: a node asked to join; requires approval.</li><li><strong>Paired node</strong>: approved node with an issued auth token.</li><li><strong>Transport</strong>: the Gateway WS endpoint forwards requests but does not decide membership. (Legacy TCP bridge support is deprecated/removed.)</li></ul><h2 id="how-pairing-works" tabindex="-1"><a class="header-anchor" href="#how-pairing-works">#</a> How pairing works</h2><ol><li>A node connects to the Gateway WS and requests pairing.</li><li>The Gateway stores a <strong>pending request</strong> and emits <code>node.pair.requested</code>.</li><li>You approve or reject the request (CLI or UI).</li><li>On approval, the Gateway issues a <strong>new token</strong> (tokens are rotated on re‑pair).</li><li>The node reconnects using the token and is now “paired”.</li></ol><p>Pending requests expire automatically after <strong>5 minutes</strong>.</p><h2 id="cli-workflow-headless-friendly" tabindex="-1"><a class="header-anchor" href="#cli-workflow-headless-friendly">#</a> CLI workflow (headless friendly)</h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nodes</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pending</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nodes</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> approve</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">requestI</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nodes</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> reject</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">requestI</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nodes</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nodes</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rename</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --node</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">--name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Living Room iPad&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p><code>nodes status</code> shows paired/connected nodes and their capabilities.</p><h2 id="api-surface-gateway-protocol" tabindex="-1"><a class="header-anchor" href="#api-surface-gateway-protocol">#</a> API surface (gateway protocol)</h2><p>Events:</p><ul><li><code>node.pair.requested</code> — emitted when a new pending request is created.</li><li><code>node.pair.resolved</code> — emitted when a request is approved/rejected/expired.</li></ul><p>Methods:</p><ul><li><code>node.pair.request</code> — create or reuse a pending request.</li><li><code>node.pair.list</code> — list pending + paired nodes.</li><li><code>node.pair.approve</code> — approve a pending request (issues token).</li><li><code>node.pair.reject</code> — reject a pending request.</li><li><code>node.pair.verify</code> — verify <code>{ nodeId, token }</code>.</li></ul><p>Notes:</p><ul><li><code>node.pair.request</code> is idempotent per node: repeated calls return the same pending request.</li><li>Approval <strong>always</strong> generates a fresh token; no token is ever returned from <code>node.pair.request</code>.</li><li>Requests may include <code>silent: true</code> as a hint for auto-approval flows.</li></ul><h2 id="auto-approval-macos-app" tabindex="-1"><a class="header-anchor" href="#auto-approval-macos-app">#</a> Auto-approval (macOS app)</h2><p>The macOS app can optionally attempt a <strong>silent approval</strong> when:</p><ul><li>the request is marked <code>silent</code>, and</li><li>the app can verify an SSH connection to the gateway host using the same user.</li></ul><p>If silent approval fails, it falls back to the normal “Approve/Reject” prompt.</p><h2 id="storage-local-private" tabindex="-1"><a class="header-anchor" href="#storage-local-private">#</a> Storage (local, private)</h2><p>Pairing state is stored under the Gateway state directory (default <code>~/.opensoul</code>):</p><ul><li><code>~/.opensoul/nodes/paired.json</code></li><li><code>~/.opensoul/nodes/pending.json</code></li></ul><p>If you override <code>OPENSOUL_STATE_DIR</code>, the <code>nodes/</code> folder moves with it.</p><p>Security notes:</p><ul><li>Tokens are secrets; treat <code>paired.json</code> as sensitive.</li><li>Rotating a token requires re-approval (or deleting the node entry).</li></ul><h2 id="transport-behavior" tabindex="-1"><a class="header-anchor" href="#transport-behavior">#</a> Transport behavior</h2><ul><li>The transport is <strong>stateless</strong>; it does not store membership.</li><li>If the Gateway is offline or pairing is disabled, nodes cannot pair.</li><li>If the Gateway is in remote mode, pairing still happens against the remote Gateway’s store.</li></ul>`,30)])])}const k=s(t,[["render",o]]);export{g as __pageData,k as default};
