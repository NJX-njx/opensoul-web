import{_ as e,o as i,c as n,ai as o}from"./chunks/framework.CsYGeNYI.js";const u=JSON.parse('{"title":"Session Tools","description":"","frontmatter":{"summary":"Agent session tools for listing sessions, fetching history, and sending cross-session messages","read_when":["Adding or modifying session tools"],"title":"Session Tools"},"headers":[],"relativePath":"concepts/session-tool.md","filePath":"concepts/session-tool.md","lastUpdated":1771906665000}'),a={name:"concepts/session-tool.md"};function l(t,s,d,c,r,p){return i(),n("div",null,[...s[0]||(s[0]=[o(`<h1 id="session-tools" tabindex="-1"><a class="header-anchor" href="#session-tools">#</a> Session Tools</h1><p>Goal: small, hard-to-misuse tool set so agents can list sessions, fetch history, and send to another session.</p><h2 id="tool-names" tabindex="-1"><a class="header-anchor" href="#tool-names">#</a> Tool Names</h2><ul><li><code>sessions_list</code></li><li><code>sessions_history</code></li><li><code>sessions_send</code></li><li><code>sessions_spawn</code></li></ul><h2 id="key-model" tabindex="-1"><a class="header-anchor" href="#key-model">#</a> Key Model</h2><ul><li>Main direct chat bucket is always the literal key <code>&quot;main&quot;</code> (resolved to the current agent’s main key).</li><li>Group chats use <code>agent:&lt;agentId&gt;:&lt;channel&gt;:group:&lt;id&gt;</code> or <code>agent:&lt;agentId&gt;:&lt;channel&gt;:channel:&lt;id&gt;</code> (pass the full key).</li><li>Cron jobs use <code>cron:&lt;job.id&gt;</code>.</li><li>Hooks use <code>hook:&lt;uuid&gt;</code> unless explicitly set.</li><li>Node sessions use <code>node-&lt;nodeId&gt;</code> unless explicitly set.</li></ul><p><code>global</code> and <code>unknown</code> are reserved values and are never listed. If <code>session.scope = &quot;global&quot;</code>, we alias it to <code>main</code> for all tools so callers never see <code>global</code>.</p><h2 id="sessions-list" tabindex="-1"><a class="header-anchor" href="#sessions-list">#</a> sessions_list</h2><p>List sessions as an array of rows.</p><p>Parameters:</p><ul><li><code>kinds?: string[]</code> filter: any of <code>&quot;main&quot; | &quot;group&quot; | &quot;cron&quot; | &quot;hook&quot; | &quot;node&quot; | &quot;other&quot;</code></li><li><code>limit?: number</code> max rows (default: server default, clamp e.g. 200)</li><li><code>activeMinutes?: number</code> only sessions updated within N minutes</li><li><code>messageLimit?: number</code> 0 = no messages (default 0); &gt;0 = include last N messages</li></ul><p>Behavior:</p><ul><li><code>messageLimit &gt; 0</code> fetches <code>chat.history</code> per session and includes the last N messages.</li><li>Tool results are filtered out in list output; use <code>sessions_history</code> for tool messages.</li><li>When running in a <strong>sandboxed</strong> agent session, session tools default to <strong>spawned-only visibility</strong> (see below).</li></ul><p>Row shape (JSON):</p><ul><li><code>key</code>: session key (string)</li><li><code>kind</code>: <code>main | group | cron | hook | node | other</code></li><li><code>channel</code>: <code>whatsapp | telegram | discord | signal | imessage | webchat | internal | unknown</code></li><li><code>displayName</code> (group display label if available)</li><li><code>updatedAt</code> (ms)</li><li><code>sessionId</code></li><li><code>model</code>, <code>contextTokens</code>, <code>totalTokens</code></li><li><code>thinkingLevel</code>, <code>verboseLevel</code>, <code>systemSent</code>, <code>abortedLastRun</code></li><li><code>sendPolicy</code> (session override if set)</li><li><code>lastChannel</code>, <code>lastTo</code></li><li><code>deliveryContext</code> (normalized <code>{ channel, to, accountId }</code> when available)</li><li><code>transcriptPath</code> (best-effort path derived from store dir + sessionId)</li><li><code>messages?</code> (only when <code>messageLimit &gt; 0</code>)</li></ul><h2 id="sessions-history" tabindex="-1"><a class="header-anchor" href="#sessions-history">#</a> sessions_history</h2><p>Fetch transcript for one session.</p><p>Parameters:</p><ul><li><code>sessionKey</code> (required; accepts session key or <code>sessionId</code> from <code>sessions_list</code>)</li><li><code>limit?: number</code> max messages (server clamps)</li><li><code>includeTools?: boolean</code> (default false)</li></ul><p>Behavior:</p><ul><li><code>includeTools=false</code> filters <code>role: &quot;toolResult&quot;</code> messages.</li><li>Returns messages array in the raw transcript format.</li><li>When given a <code>sessionId</code>, OpenSoul resolves it to the corresponding session key (missing ids error).</li></ul><h2 id="sessions-send" tabindex="-1"><a class="header-anchor" href="#sessions-send">#</a> sessions_send</h2><p>Send a message into another session.</p><p>Parameters:</p><ul><li><code>sessionKey</code> (required; accepts session key or <code>sessionId</code> from <code>sessions_list</code>)</li><li><code>message</code> (required)</li><li><code>timeoutSeconds?: number</code> (default &gt;0; 0 = fire-and-forget)</li></ul><p>Behavior:</p><ul><li><code>timeoutSeconds = 0</code>: enqueue and return <code>{ runId, status: &quot;accepted&quot; }</code>.</li><li><code>timeoutSeconds &gt; 0</code>: wait up to N seconds for completion, then return <code>{ runId, status: &quot;ok&quot;, reply }</code>.</li><li>If wait times out: <code>{ runId, status: &quot;timeout&quot;, error }</code>. Run continues; call <code>sessions_history</code> later.</li><li>If the run fails: <code>{ runId, status: &quot;error&quot;, error }</code>.</li><li>Announce delivery runs after the primary run completes and is best-effort; <code>status: &quot;ok&quot;</code> does not guarantee the announce was delivered.</li><li>Waits via gateway <code>agent.wait</code> (server-side) so reconnects don&#39;t drop the wait.</li><li>Agent-to-agent message context is injected for the primary run.</li><li>After the primary run completes, OpenSoul runs a <strong>reply-back loop</strong>: <ul><li>Round 2+ alternates between requester and target agents.</li><li>Reply exactly <code>REPLY_SKIP</code> to stop the ping‑pong.</li><li>Max turns is <code>session.agentToAgent.maxPingPongTurns</code> (0–5, default 5).</li></ul></li><li>Once the loop ends, OpenSoul runs the <strong>agent‑to‑agent announce step</strong> (target agent only): <ul><li>Reply exactly <code>ANNOUNCE_SKIP</code> to stay silent.</li><li>Any other reply is sent to the target channel.</li><li>Announce step includes the original request + round‑1 reply + latest ping‑pong reply.</li></ul></li></ul><h2 id="channel-field" tabindex="-1"><a class="header-anchor" href="#channel-field">#</a> Channel Field</h2><ul><li>For groups, <code>channel</code> is the channel recorded on the session entry.</li><li>For direct chats, <code>channel</code> maps from <code>lastChannel</code>.</li><li>For cron/hook/node, <code>channel</code> is <code>internal</code>.</li><li>If missing, <code>channel</code> is <code>unknown</code>.</li></ul><h2 id="security-send-policy" tabindex="-1"><a class="header-anchor" href="#security-send-policy">#</a> Security / Send Policy</h2><p>Policy-based blocking by channel/chat type (not per session id).</p><div class="language-json vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;session&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;sendPolicy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;rules&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          &quot;match&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;channel&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;discord&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;chatType&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;group&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          &quot;action&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;deny&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;default&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;allow&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>Runtime override (per session entry):</p><ul><li><code>sendPolicy: &quot;allow&quot; | &quot;deny&quot;</code> (unset = inherit config)</li><li>Settable via <code>sessions.patch</code> or owner-only <code>/send on|off|inherit</code> (standalone message).</li></ul><p>Enforcement points:</p><ul><li><code>chat.send</code> / <code>agent</code> (gateway)</li><li>auto-reply delivery logic</li></ul><h2 id="sessions-spawn" tabindex="-1"><a class="header-anchor" href="#sessions-spawn">#</a> sessions_spawn</h2><p>Spawn a sub-agent run in an isolated session and announce the result back to the requester chat channel.</p><p>Parameters:</p><ul><li><code>task</code> (required)</li><li><code>label?</code> (optional; used for logs/UI)</li><li><code>agentId?</code> (optional; spawn under another agent id if allowed)</li><li><code>model?</code> (optional; overrides the sub-agent model; invalid values error)</li><li><code>runTimeoutSeconds?</code> (default 0; when set, aborts the sub-agent run after N seconds)</li><li><code>cleanup?</code> (<code>delete|keep</code>, default <code>keep</code>)</li></ul><p>Allowlist:</p><ul><li><code>agents.list[].subagents.allowAgents</code>: list of agent ids allowed via <code>agentId</code> (<code>[&quot;*&quot;]</code> to allow any). Default: only the requester agent.</li></ul><p>Discovery:</p><ul><li>Use <code>agents_list</code> to discover which agent ids are allowed for <code>sessions_spawn</code>.</li></ul><p>Behavior:</p><ul><li>Starts a new <code>agent:&lt;agentId&gt;:subagent:&lt;uuid&gt;</code> session with <code>deliver: false</code>.</li><li>Sub-agents default to the full tool set <strong>minus session tools</strong> (configurable via <code>tools.subagents.tools</code>).</li><li>Sub-agents are not allowed to call <code>sessions_spawn</code> (no sub-agent → sub-agent spawning).</li><li>Always non-blocking: returns <code>{ status: &quot;accepted&quot;, runId, childSessionKey }</code> immediately.</li><li>After completion, OpenSoul runs a sub-agent <strong>announce step</strong> and posts the result to the requester chat channel.</li><li>Reply exactly <code>ANNOUNCE_SKIP</code> during the announce step to stay silent.</li><li>Announce replies are normalized to <code>Status</code>/<code>Result</code>/<code>Notes</code>; <code>Status</code> comes from runtime outcome (not model text).</li><li>Sub-agent sessions are auto-archived after <code>agents.defaults.subagents.archiveAfterMinutes</code> (default: 60).</li><li>Announce replies include a stats line (runtime, tokens, sessionKey/sessionId, transcript path, and optional cost).</li></ul><h2 id="sandbox-session-visibility" tabindex="-1"><a class="header-anchor" href="#sandbox-session-visibility">#</a> Sandbox Session Visibility</h2><p>Sandboxed sessions can use session tools, but by default they only see sessions they spawned via <code>sessions_spawn</code>.</p><p>Config:</p><div class="language-json5 vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json5</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  agents</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    defaults</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // default: &quot;spawned&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        sessionToolsVisibility</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;spawned&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// or &quot;all&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div>`,50)])])}const g=e(a,[["render",l]]);export{u as __pageData,g as default};
