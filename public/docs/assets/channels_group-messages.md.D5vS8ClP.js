import{_ as e,o as a,c as n,ai as i}from"./chunks/framework.CsYGeNYI.js";const g=JSON.parse('{"title":"Group Messages","description":"","frontmatter":{"summary":"Behavior and config for WhatsApp group message handling (mentionPatterns are shared across surfaces)","read_when":["Changing group message rules or mentions"],"title":"Group Messages"},"headers":[],"relativePath":"channels/group-messages.md","filePath":"channels/group-messages.md","lastUpdated":null}'),t={name:"channels/group-messages.md"};function o(l,s,p,r,h,d){return a(),n("div",null,[...s[0]||(s[0]=[i(`<h1 id="group-messages-whatsapp-web-channel" tabindex="-1"><a class="header-anchor" href="#group-messages-whatsapp-web-channel">#</a> Group messages (WhatsApp web channel)</h1><p>Goal: let Clawd sit in WhatsApp groups, wake up only when pinged, and keep that thread separate from the personal DM session.</p><p>Note: <code>agents.list[].groupChat.mentionPatterns</code> is now used by Telegram/Discord/Slack/iMessage as well; this doc focuses on WhatsApp-specific behavior. For multi-agent setups, set <code>agents.list[].groupChat.mentionPatterns</code> per agent (or use <code>messages.groupChat.mentionPatterns</code> as a global fallback).</p><h2 id="what-s-implemented-2025-12-03" tabindex="-1"><a class="header-anchor" href="#what-s-implemented-2025-12-03">#</a> What’s implemented (2025-12-03)</h2><ul><li>Activation modes: <code>mention</code> (default) or <code>always</code>. <code>mention</code> requires a ping (real WhatsApp @-mentions via <code>mentionedJids</code>, regex patterns, or the bot’s E.164 anywhere in the text). <code>always</code> wakes the agent on every message but it should reply only when it can add meaningful value; otherwise it returns the silent token <code>NO_REPLY</code>. Defaults can be set in config (<code>channels.whatsapp.groups</code>) and overridden per group via <code>/activation</code>. When <code>channels.whatsapp.groups</code> is set, it also acts as a group allowlist (include <code>&quot;*&quot;</code> to allow all).</li><li>Group policy: <code>channels.whatsapp.groupPolicy</code> controls whether group messages are accepted (<code>open|disabled|allowlist</code>). <code>allowlist</code> uses <code>channels.whatsapp.groupAllowFrom</code> (fallback: explicit <code>channels.whatsapp.allowFrom</code>). Default is <code>allowlist</code> (blocked until you add senders).</li><li>Per-group sessions: session keys look like <code>agent:&lt;agentId&gt;:whatsapp:group:&lt;jid&gt;</code> so commands such as <code>/verbose on</code> or <code>/think high</code> (sent as standalone messages) are scoped to that group; personal DM state is untouched. Heartbeats are skipped for group threads.</li><li>Context injection: <strong>pending-only</strong> group messages (default 50) that <em>did not</em> trigger a run are prefixed under <code>[Chat messages since your last reply - for context]</code>, with the triggering line under <code>[Current message - respond to this]</code>. Messages already in the session are not re-injected.</li><li>Sender surfacing: every group batch now ends with <code>[from: Sender Name (+E164)]</code> so Pi knows who is speaking.</li><li>Ephemeral/view-once: we unwrap those before extracting text/mentions, so pings inside them still trigger.</li><li>Group system prompt: on the first turn of a group session (and whenever <code>/activation</code> changes the mode) we inject a short blurb into the system prompt like <code>You are replying inside the WhatsApp group &quot;&lt;subject&gt;&quot;. Group members: Alice (+44...), Bob (+43...), … Activation: trigger-only … Address the specific sender noted in the message context.</code> If metadata isn’t available we still tell the agent it’s a group chat.</li></ul><h2 id="config-example-whatsapp" tabindex="-1"><a class="header-anchor" href="#config-example-whatsapp">#</a> Config example (WhatsApp)</h2><p>Add a <code>groupChat</code> block to <code>~/.opensoul/opensoul.json</code> so display-name pings work even when WhatsApp strips the visual <code>@</code> in the text body:</p><div class="language-json5 vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json5</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  channels</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    whatsapp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      groups</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;*&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">requireMention</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  agents</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    list</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;main&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        groupChat</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          historyLimit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          mentionPatterns</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@?opensoul&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\\\</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">+?15555550123&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br></div></div><p>Notes:</p><ul><li>The regexes are case-insensitive; they cover a display-name ping like <code>@opensoul</code> and the raw number with or without <code>+</code>/spaces.</li><li>WhatsApp still sends canonical mentions via <code>mentionedJids</code> when someone taps the contact, so the number fallback is rarely needed but is a useful safety net.</li></ul><h3 id="activation-command-owner-only" tabindex="-1"><a class="header-anchor" href="#activation-command-owner-only">#</a> Activation command (owner-only)</h3><p>Use the group chat command:</p><ul><li><code>/activation mention</code></li><li><code>/activation always</code></li></ul><p>Only the owner number (from <code>channels.whatsapp.allowFrom</code>, or the bot’s own E.164 when unset) can change this. Send <code>/status</code> as a standalone message in the group to see the current activation mode.</p><h2 id="how-to-use" tabindex="-1"><a class="header-anchor" href="#how-to-use">#</a> How to use</h2><ol><li>Add your WhatsApp account (the one running OpenSoul) to the group.</li><li>Say <code>@opensoul …</code> (or include the number). Only allowlisted senders can trigger it unless you set <code>groupPolicy: &quot;open&quot;</code>.</li><li>The agent prompt will include recent group context plus the trailing <code>[from: …]</code> marker so it can address the right person.</li><li>Session-level directives (<code>/verbose on</code>, <code>/think high</code>, <code>/new</code> or <code>/reset</code>, <code>/compact</code>) apply only to that group’s session; send them as standalone messages so they register. Your personal DM session remains independent.</li></ol><h2 id="testing-verification" tabindex="-1"><a class="header-anchor" href="#testing-verification">#</a> Testing / verification</h2><ul><li>Manual smoke: <ul><li>Send an <code>@opensoul</code> ping in the group and confirm a reply that references the sender name.</li><li>Send a second ping and verify the history block is included then cleared on the next turn.</li></ul></li><li>Check gateway logs (run with <code>--verbose</code>) to see <code>inbound web message</code> entries showing <code>from: &lt;groupJid&gt;</code> and the <code>[from: …]</code> suffix.</li></ul><h2 id="known-considerations" tabindex="-1"><a class="header-anchor" href="#known-considerations">#</a> Known considerations</h2><ul><li>Heartbeats are intentionally skipped for groups to avoid noisy broadcasts.</li><li>Echo suppression uses the combined batch string; if you send identical text twice without mentions, only the first will get a response.</li><li>Session store entries will appear as <code>agent:&lt;agentId&gt;:whatsapp:group:&lt;jid&gt;</code> in the session store (<code>~/.opensoul/agents/&lt;agentId&gt;/sessions/sessions.json</code> by default); a missing entry just means the group hasn’t triggered a run yet.</li><li>Typing indicators in groups follow <code>agents.defaults.typingMode</code> (default: <code>message</code> when unmentioned).</li></ul>`,20)])])}const u=e(t,[["render",o]]);export{g as __pageData,u as default};
