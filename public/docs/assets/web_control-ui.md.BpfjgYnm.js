import{_ as s,o as a,c as i,ai as t}from"./chunks/framework.CsYGeNYI.js";const u=JSON.parse('{"title":"Control UI","description":"","frontmatter":{"summary":"Browser-based control UI for the Gateway (chat, nodes, config)","read_when":["You want to operate the Gateway from a browser","You want Tailnet access without SSH tunnels"],"title":"Control UI"},"headers":[],"relativePath":"web/control-ui.md","filePath":"web/control-ui.md","lastUpdated":1771906665000}'),n={name:"web/control-ui.md"};function l(o,e,r,d,p,c){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="control-ui-browser" tabindex="-1"><a class="header-anchor" href="#control-ui-browser">#</a> Control UI (browser)</h1><p>The Control UI is a small <strong>Vite + Lit</strong> single-page app served by the Gateway:</p><ul><li>default: <code>http://&lt;host&gt;:18789/</code></li><li>optional prefix: set <code>gateway.controlUi.basePath</code> (e.g. <code>/opensoul</code>)</li></ul><p>It speaks <strong>directly to the Gateway WebSocket</strong> on the same port.</p><h2 id="quick-open-local" tabindex="-1"><a class="header-anchor" href="#quick-open-local">#</a> Quick open (local)</h2><p>If the Gateway is running on the same computer, open:</p><ul><li><a href="http://127.0.0.1:18789/" target="_blank" rel="noreferrer">http://127.0.0.1:18789/</a> (or <code>http://localhost:18789/</code>)</li></ul><p>If the page fails to load, start the Gateway first: <code>opensoul gateway</code>.</p><p>Auth is supplied during the WebSocket handshake via:</p><ul><li><code>connect.params.auth.token</code></li><li><code>connect.params.auth.password</code> The dashboard settings panel lets you store a token; passwords are not persisted. The onboarding wizard generates a gateway token by default, so paste it here on first connect.</li></ul><h2 id="device-pairing-first-connection" tabindex="-1"><a class="header-anchor" href="#device-pairing-first-connection">#</a> Device pairing (first connection)</h2><p>When you connect to the Control UI from a new browser or device, the Gateway requires a <strong>one-time pairing approval</strong> — even if you&#39;re on the same Tailnet with <code>gateway.auth.allowTailscale: true</code>. This is a security measure to prevent unauthorized access.</p><p><strong>What you&#39;ll see:</strong> &quot;disconnected (1008): pairing required&quot;</p><p><strong>To approve the device:</strong></p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># List pending requests</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> devices</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Approve by request ID</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> devices</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> approve</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">requestI</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Once approved, the device is remembered and won&#39;t require re-approval unless you revoke it with <code>opensoul devices revoke --device &lt;id&gt; --role &lt;role&gt;</code>. See <a href="/docs/cli/devices">Devices CLI</a> for token rotation and revocation.</p><p><strong>Notes:</strong></p><ul><li>Local connections (<code>127.0.0.1</code>) are auto-approved.</li><li>Remote connections (LAN, Tailnet, etc.) require explicit approval.</li><li>Each browser profile generates a unique device ID, so switching browsers or clearing browser data will require re-pairing.</li></ul><h2 id="what-it-can-do-today" tabindex="-1"><a class="header-anchor" href="#what-it-can-do-today">#</a> What it can do (today)</h2><ul><li>Chat with the model via Gateway WS (<code>chat.history</code>, <code>chat.send</code>, <code>chat.abort</code>, <code>chat.inject</code>)</li><li>Stream tool calls + live tool output cards in Chat (agent events)</li><li>Channels: WhatsApp/Telegram/Discord/Slack + plugin channels (Mattermost, etc.) status + QR login + per-channel config (<code>channels.status</code>, <code>web.login.*</code>, <code>config.patch</code>)</li><li>Instances: presence list + refresh (<code>system-presence</code>)</li><li>Sessions: list + per-session thinking/verbose overrides (<code>sessions.list</code>, <code>sessions.patch</code>)</li><li>Cron jobs: list/add/run/enable/disable + run history (<code>cron.*</code>)</li><li>Skills: status, enable/disable, install, API key updates (<code>skills.*</code>)</li><li>Nodes: list + caps (<code>node.list</code>)</li><li>Exec approvals: edit gateway or node allowlists + ask policy for <code>exec host=gateway/node</code> (<code>exec.approvals.*</code>)</li><li>Config: view/edit <code>~/.opensoul/opensoul.json</code> (<code>config.get</code>, <code>config.set</code>)</li><li>Config: apply + restart with validation (<code>config.apply</code>) and wake the last active session</li><li>Config writes include a base-hash guard to prevent clobbering concurrent edits</li><li>Config schema + form rendering (<code>config.schema</code>, including plugin + channel schemas); Raw JSON editor remains available</li><li>Debug: status/health/models snapshots + event log + manual RPC calls (<code>status</code>, <code>health</code>, <code>models.list</code>)</li><li>Logs: live tail of gateway file logs with filter/export (<code>logs.tail</code>)</li><li>Update: run a package/git update + restart (<code>update.run</code>) with a restart report</li></ul><p>Cron jobs panel notes:</p><ul><li>For isolated jobs, delivery defaults to announce summary. You can switch to none if you want internal-only runs.</li><li>Channel/target fields appear when announce is selected.</li></ul><h2 id="chat-behavior" tabindex="-1"><a class="header-anchor" href="#chat-behavior">#</a> Chat behavior</h2><ul><li><code>chat.send</code> is <strong>non-blocking</strong>: it acks immediately with <code>{ runId, status: &quot;started&quot; }</code> and the response streams via <code>chat</code> events.</li><li>Re-sending with the same <code>idempotencyKey</code> returns <code>{ status: &quot;in_flight&quot; }</code> while running, and <code>{ status: &quot;ok&quot; }</code> after completion.</li><li><code>chat.inject</code> appends an assistant note to the session transcript and broadcasts a <code>chat</code> event for UI-only updates (no agent run, no channel delivery).</li><li>Stop: <ul><li>Click <strong>Stop</strong> (calls <code>chat.abort</code>)</li><li>Type <code>/stop</code> (or <code>stop|esc|abort|wait|exit|interrupt</code>) to abort out-of-band</li><li><code>chat.abort</code> supports <code>{ sessionKey }</code> (no <code>runId</code>) to abort all active runs for that session</li></ul></li></ul><h2 id="tailnet-access-recommended" tabindex="-1"><a class="header-anchor" href="#tailnet-access-recommended">#</a> Tailnet access (recommended)</h2><h3 id="integrated-tailscale-serve-preferred" tabindex="-1"><a class="header-anchor" href="#integrated-tailscale-serve-preferred">#</a> Integrated Tailscale Serve (preferred)</h3><p>Keep the Gateway on loopback and let Tailscale Serve proxy it with HTTPS:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --tailscale</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> serve</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Open:</p><ul><li><code>https://&lt;magicdns&gt;/</code> (or your configured <code>gateway.controlUi.basePath</code>)</li></ul><p>By default, Serve requests can authenticate via Tailscale identity headers (<code>tailscale-user-login</code>) when <code>gateway.auth.allowTailscale</code> is <code>true</code>. OpenSoul verifies the identity by resolving the <code>x-forwarded-for</code> address with <code>tailscale whois</code> and matching it to the header, and only accepts these when the request hits loopback with Tailscale’s <code>x-forwarded-*</code> headers. Set <code>gateway.auth.allowTailscale: false</code> (or force <code>gateway.auth.mode: &quot;password&quot;</code>) if you want to require a token/password even for Serve traffic.</p><h3 id="bind-to-tailnet-token" tabindex="-1"><a class="header-anchor" href="#bind-to-tailnet-token">#</a> Bind to tailnet + token</h3><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --bind</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tailnet</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --token</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">openssl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rand </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">-hex</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 32</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Then open:</p><ul><li><code>http://&lt;tailscale-ip&gt;:18789/</code> (or your configured <code>gateway.controlUi.basePath</code>)</li></ul><p>Paste the token into the UI settings (sent as <code>connect.params.auth.token</code>).</p><h2 id="insecure-http" tabindex="-1"><a class="header-anchor" href="#insecure-http">#</a> Insecure HTTP</h2><p>If you open the dashboard over plain HTTP (<code>http://&lt;lan-ip&gt;</code> or <code>http://&lt;tailscale-ip&gt;</code>), the browser runs in a <strong>non-secure context</strong> and blocks WebCrypto. By default, OpenSoul <strong>blocks</strong> Control UI connections without device identity.</p><p><strong>Recommended fix:</strong> use HTTPS (Tailscale Serve) or open the UI locally:</p><ul><li><code>https://&lt;magicdns&gt;/</code> (Serve)</li><li><code>http://127.0.0.1:18789/</code> (on the gateway host)</li></ul><p><strong>Downgrade example (token-only over HTTP):</strong></p><div class="language-json5 vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json5</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  gateway</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    controlUi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">allowInsecureAuth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    bind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;tailnet&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    auth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;token&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">token</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;replace-me&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>This disables device identity + pairing for the Control UI (even on HTTPS). Use only if you trust the network.</p><p>See <a href="/docs/gateway/tailscale">Tailscale</a> for HTTPS setup guidance.</p><h2 id="building-the-ui" tabindex="-1"><a class="header-anchor" href="#building-the-ui">#</a> Building the UI</h2><p>The Gateway serves static files from <code>dist/control-ui</code>. Build them with:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ui:build</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # auto-installs UI deps on first run</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Optional absolute base (when you want fixed asset URLs):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OPENSOUL_CONTROL_UI_BASE_PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/opensoul/</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ui:build</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>For local development (separate dev server):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ui:dev</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # auto-installs UI deps on first run</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Then point the UI at your Gateway WS URL (e.g. <code>ws://127.0.0.1:18789</code>).</p><h2 id="debugging-testing-dev-server-remote-gateway" tabindex="-1"><a class="header-anchor" href="#debugging-testing-dev-server-remote-gateway">#</a> Debugging/testing: dev server + remote Gateway</h2><p>The Control UI is static files; the WebSocket target is configurable and can be different from the HTTP origin. This is handy when you want the Vite dev server locally but the Gateway runs elsewhere.</p><ol><li>Start the UI dev server: <code>pnpm ui:dev</code></li><li>Open a URL like:</li></ol><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>http://localhost:5173/?gatewayUrl=ws://&lt;gateway-host&gt;:18789</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Optional one-time auth (if needed):</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>http://localhost:5173/?gatewayUrl=wss://&lt;gateway-host&gt;:18789&amp;token=&lt;gateway-token&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Notes:</p><ul><li><code>gatewayUrl</code> is stored in localStorage after load and removed from the URL.</li><li><code>token</code> is stored in localStorage; <code>password</code> is kept in memory only.</li><li>When <code>gatewayUrl</code> is set, the UI does not fall back to config or environment credentials. Provide <code>token</code> (or <code>password</code>) explicitly. Missing explicit credentials is an error.</li><li>Use <code>wss://</code> when the Gateway is behind TLS (Tailscale Serve, HTTPS proxy, etc.).</li><li><code>gatewayUrl</code> is only accepted in a top-level window (not embedded) to prevent clickjacking.</li><li>For cross-origin dev setups (e.g. <code>pnpm ui:dev</code> to a remote Gateway), add the UI origin to <code>gateway.controlUi.allowedOrigins</code>.</li></ul><p>Example:</p><div class="language-json5 vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json5</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  gateway</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    controlUi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      allowedOrigins</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;http://localhost:5173&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Remote access setup details: <a href="/docs/gateway/remote">Remote access</a>.</p>`,63)])])}const k=s(n,[["render",l]]);export{u as __pageData,k as default};
