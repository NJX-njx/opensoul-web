import{_ as e,o as a,c as n,ai as i}from"./chunks/framework.CsYGeNYI.js";const u=JSON.parse('{"title":"Sandboxing","description":"","frontmatter":{"summary":"How OpenSoul sandboxing works: modes, scopes, workspace access, and images","title":"Sandboxing","read_when":"You want a dedicated explanation of sandboxing or need to tune agents.defaults.sandbox.","status":"active"},"headers":[],"relativePath":"gateway/sandboxing.md","filePath":"gateway/sandboxing.md","lastUpdated":1771906665000}'),o={name:"gateway/sandboxing.md"};function t(l,s,d,r,c,p){return a(),n("div",null,[...s[0]||(s[0]=[i(`<h1 id="sandboxing" tabindex="-1"><a class="header-anchor" href="#sandboxing">#</a> Sandboxing</h1><p>OpenSoul can run <strong>tools inside Docker containers</strong> to reduce blast radius. This is <strong>optional</strong> and controlled by configuration (<code>agents.defaults.sandbox</code> or <code>agents.list[].sandbox</code>). If sandboxing is off, tools run on the host. The Gateway stays on the host; tool execution runs in an isolated sandbox when enabled.</p><p>This is not a perfect security boundary, but it materially limits filesystem and process access when the model does something dumb.</p><h2 id="what-gets-sandboxed" tabindex="-1"><a class="header-anchor" href="#what-gets-sandboxed">#</a> What gets sandboxed</h2><ul><li>Tool execution (<code>exec</code>, <code>read</code>, <code>write</code>, <code>edit</code>, <code>apply_patch</code>, <code>process</code>, etc.).</li><li>Optional sandboxed browser (<code>agents.defaults.sandbox.browser</code>). <ul><li>By default, the sandbox browser auto-starts (ensures CDP is reachable) when the browser tool needs it. Configure via <code>agents.defaults.sandbox.browser.autoStart</code> and <code>agents.defaults.sandbox.browser.autoStartTimeoutMs</code>.</li><li><code>agents.defaults.sandbox.browser.allowHostControl</code> lets sandboxed sessions target the host browser explicitly.</li><li>Optional allowlists gate <code>target: &quot;custom&quot;</code>: <code>allowedControlUrls</code>, <code>allowedControlHosts</code>, <code>allowedControlPorts</code>.</li></ul></li></ul><p>Not sandboxed:</p><ul><li>The Gateway process itself.</li><li>Any tool explicitly allowed to run on the host (e.g. <code>tools.elevated</code>). <ul><li><strong>Elevated exec runs on the host and bypasses sandboxing.</strong></li><li>If sandboxing is off, <code>tools.elevated</code> does not change execution (already on host). See <a href="/docs/tools/elevated">Elevated Mode</a>.</li></ul></li></ul><h2 id="modes" tabindex="-1"><a class="header-anchor" href="#modes">#</a> Modes</h2><p><code>agents.defaults.sandbox.mode</code> controls <strong>when</strong> sandboxing is used:</p><ul><li><code>&quot;off&quot;</code>: no sandboxing.</li><li><code>&quot;non-main&quot;</code>: sandbox only <strong>non-main</strong> sessions (default if you want normal chats on host).</li><li><code>&quot;all&quot;</code>: every session runs in a sandbox. Note: <code>&quot;non-main&quot;</code> is based on <code>session.mainKey</code> (default <code>&quot;main&quot;</code>), not agent id. Group/channel sessions use their own keys, so they count as non-main and will be sandboxed.</li></ul><h2 id="scope" tabindex="-1"><a class="header-anchor" href="#scope">#</a> Scope</h2><p><code>agents.defaults.sandbox.scope</code> controls <strong>how many containers</strong> are created:</p><ul><li><code>&quot;session&quot;</code> (default): one container per session.</li><li><code>&quot;agent&quot;</code>: one container per agent.</li><li><code>&quot;shared&quot;</code>: one container shared by all sandboxed sessions.</li></ul><h2 id="workspace-access" tabindex="-1"><a class="header-anchor" href="#workspace-access">#</a> Workspace access</h2><p><code>agents.defaults.sandbox.workspaceAccess</code> controls <strong>what the sandbox can see</strong>:</p><ul><li><code>&quot;none&quot;</code> (default): tools see a sandbox workspace under <code>~/.opensoul/sandboxes</code>.</li><li><code>&quot;ro&quot;</code>: mounts the agent workspace read-only at <code>/agent</code> (disables <code>write</code>/<code>edit</code>/<code>apply_patch</code>).</li><li><code>&quot;rw&quot;</code>: mounts the agent workspace read/write at <code>/workspace</code>.</li></ul><p>Inbound media is copied into the active sandbox workspace (<code>media/inbound/*</code>). Skills note: the <code>read</code> tool is sandbox-rooted. With <code>workspaceAccess: &quot;none&quot;</code>, OpenSoul mirrors eligible skills into the sandbox workspace (<code>.../skills</code>) so they can be read. With <code>&quot;rw&quot;</code>, workspace skills are readable from <code>/workspace/skills</code>.</p><h2 id="custom-bind-mounts" tabindex="-1"><a class="header-anchor" href="#custom-bind-mounts">#</a> Custom bind mounts</h2><p><code>agents.defaults.sandbox.docker.binds</code> mounts additional host directories into the container. Format: <code>host:container:mode</code> (e.g., <code>&quot;/home/user/source:/source:rw&quot;</code>).</p><p>Global and per-agent binds are <strong>merged</strong> (not replaced). Under <code>scope: &quot;shared&quot;</code>, per-agent binds are ignored.</p><p>Example (read-only source + docker socket):</p><div class="language-json5 vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json5</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  agents</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    defaults</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        docker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          binds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/home/user/source:/source:ro&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/var/run/docker.sock:/var/run/docker.sock&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    list</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;build&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          docker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            binds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/mnt/cache:/cache:rw&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br></div></div><p>Security notes:</p><ul><li>Binds bypass the sandbox filesystem: they expose host paths with whatever mode you set (<code>:ro</code> or <code>:rw</code>).</li><li>Sensitive mounts (e.g., <code>docker.sock</code>, secrets, SSH keys) should be <code>:ro</code> unless absolutely required.</li><li>Combine with <code>workspaceAccess: &quot;ro&quot;</code> if you only need read access to the workspace; bind modes stay independent.</li><li>See <a href="/docs/gateway/sandbox-vs-tool-policy-vs-elevated">Sandbox vs Tool Policy vs Elevated</a> for how binds interact with tool policy and elevated exec.</li></ul><h2 id="images-setup" tabindex="-1"><a class="header-anchor" href="#images-setup">#</a> Images + setup</h2><p>Default image: <code>opensoul-sandbox:bookworm-slim</code></p><p>Build it once:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/sandbox-setup.sh</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Note: the default image does <strong>not</strong> include Node. If a skill needs Node (or other runtimes), either bake a custom image or install via <code>sandbox.docker.setupCommand</code> (requires network egress + writable root + root user).</p><p>Sandboxed browser image:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/sandbox-browser-setup.sh</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>By default, sandbox containers run with <strong>no network</strong>. Override with <code>agents.defaults.sandbox.docker.network</code>.</p><p>Docker installs and the containerized gateway live here: <a href="/docs/install/docker">Docker</a></p><h2 id="setupcommand-one-time-container-setup" tabindex="-1"><a class="header-anchor" href="#setupcommand-one-time-container-setup">#</a> setupCommand (one-time container setup)</h2><p><code>setupCommand</code> runs <strong>once</strong> after the sandbox container is created (not on every run). It executes inside the container via <code>sh -lc</code>.</p><p>Paths:</p><ul><li>Global: <code>agents.defaults.sandbox.docker.setupCommand</code></li><li>Per-agent: <code>agents.list[].sandbox.docker.setupCommand</code></li></ul><p>Common pitfalls:</p><ul><li>Default <code>docker.network</code> is <code>&quot;none&quot;</code> (no egress), so package installs will fail.</li><li><code>readOnlyRoot: true</code> prevents writes; set <code>readOnlyRoot: false</code> or bake a custom image.</li><li><code>user</code> must be root for package installs (omit <code>user</code> or set <code>user: &quot;0:0&quot;</code>).</li><li>Sandbox exec does <strong>not</strong> inherit host <code>process.env</code>. Use <code>agents.defaults.sandbox.docker.env</code> (or a custom image) for skill API keys.</li></ul><h2 id="tool-policy-escape-hatches" tabindex="-1"><a class="header-anchor" href="#tool-policy-escape-hatches">#</a> Tool policy + escape hatches</h2><p>Tool allow/deny policies still apply before sandbox rules. If a tool is denied globally or per-agent, sandboxing doesn’t bring it back.</p><p><code>tools.elevated</code> is an explicit escape hatch that runs <code>exec</code> on the host. <code>/exec</code> directives only apply for authorized senders and persist per session; to hard-disable <code>exec</code>, use tool policy deny (see <a href="/docs/gateway/sandbox-vs-tool-policy-vs-elevated">Sandbox vs Tool Policy vs Elevated</a>).</p><p>Debugging:</p><ul><li>Use <code>opensoul sandbox explain</code> to inspect effective sandbox mode, tool policy, and fix-it config keys.</li><li>See <a href="/docs/gateway/sandbox-vs-tool-policy-vs-elevated">Sandbox vs Tool Policy vs Elevated</a> for the “why is this blocked?” mental model. Keep it locked down.</li></ul><h2 id="multi-agent-overrides" tabindex="-1"><a class="header-anchor" href="#multi-agent-overrides">#</a> Multi-agent overrides</h2><p>Each agent can override sandbox + tools: <code>agents.list[].sandbox</code> and <code>agents.list[].tools</code> (plus <code>agents.list[].tools.sandbox.tools</code> for sandbox tool policy). See <a href="/docs/tools/multi-agent-sandbox-tools">Multi-Agent Sandbox &amp; Tools</a> for precedence.</p><h2 id="minimal-enable-example" tabindex="-1"><a class="header-anchor" href="#minimal-enable-example">#</a> Minimal enable example</h2><div class="language-json5 vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json5</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  agents</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    defaults</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      sandbox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;non-main&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        scope</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;session&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        workspaceAccess</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;none&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><h2 id="related-docs" tabindex="-1"><a class="header-anchor" href="#related-docs">#</a> Related docs</h2><ul><li><a href="/docs/gateway/configuration#agentsdefaults-sandbox">Sandbox Configuration</a></li><li><a href="/docs/tools/multi-agent-sandbox-tools">Multi-Agent Sandbox &amp; Tools</a></li><li><a href="/docs/gateway/security/index">Security</a></li></ul>`,50)])])}const k=e(o,[["render",t]]);export{u as __pageData,k as default};
