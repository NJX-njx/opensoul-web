import{_ as s,o as a,c as i,ai as l}from"./chunks/framework.CsYGeNYI.js";const u=JSON.parse('{"title":"Gateway 网关运行手册","description":"","frontmatter":{"read_when":["运行或调试 Gateway 网关进程时"],"summary":"Gateway 网关服务、生命周期和运维的运行手册","title":"Gateway 网关运行手册","x-i18n":{"generated_at":"2026-02-03T07:50:03Z","model":"claude-opus-4-5","provider":"pi","source_hash":"497d58090faaa6bdae62780ce887b40a1ad81e2e99ff186ea2a5c2249c35d9ba","source_path":"gateway/index.md","workflow":15}},"headers":[],"relativePath":"zh-CN/gateway/index.md","filePath":"zh-CN/gateway/index.md","lastUpdated":null}'),n={name:"zh-CN/gateway/index.md"};function o(t,e,d,c,p,r){return a(),i("div",null,[...e[0]||(e[0]=[l(`<h1 id="gateway-网关服务运行手册" tabindex="-1"><a class="header-anchor" href="#gateway-网关服务运行手册">#</a> Gateway 网关服务运行手册</h1><p>最后更新：2025-12-09</p><h2 id="是什么" tabindex="-1"><a class="header-anchor" href="#是什么">#</a> 是什么</h2><ul><li>拥有单一 Baileys/Telegram 连接和控制/事件平面的常驻进程。</li><li>替代旧版 <code>gateway</code> 命令。CLI 入口点：<code>opensoul gateway</code>。</li><li>运行直到停止；出现致命错误时以非零退出码退出，以便 supervisor 重启它。</li></ul><h2 id="如何运行-本地" tabindex="-1"><a class="header-anchor" href="#如何运行-本地">#</a> 如何运行（本地）</h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --port</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 18789</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 在 stdio 中获取完整的调试/追踪日志：</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --port</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 18789</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --verbose</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 如果端口被占用，终止监听器然后启动：</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --force</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 开发循环（TS 更改时自动重载）：</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway:watch</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><ul><li>配置热重载监视 <code>~/.opensoul/opensoul.json</code>（或 <code>OPENSOUL_CONFIG_PATH</code>）。 <ul><li>默认模式：<code>gateway.reload.mode=&quot;hybrid&quot;</code>（热应用安全更改，关键更改时重启）。</li><li>热重载在需要时通过 <strong>SIGUSR1</strong> 使用进程内重启。</li><li>使用 <code>gateway.reload.mode=&quot;off&quot;</code> 禁用。</li></ul></li><li>将 WebSocket 控制平面绑定到 <code>127.0.0.1:&lt;port&gt;</code>（默认 18789）。</li><li>同一端口也提供 HTTP 服务（控制界面、hooks、A2UI）。单端口多路复用。 <ul><li>OpenAI Chat Completions（HTTP）：<a href="/docs/gateway/openai-http-api"><code>/v1/chat/completions</code></a>。</li><li>OpenResponses（HTTP）：<a href="/docs/gateway/openresponses-http-api"><code>/v1/responses</code></a>。</li><li>Tools Invoke（HTTP）：<a href="/docs/gateway/tools-invoke-http-api"><code>/tools/invoke</code></a>。</li></ul></li><li>默认在 <code>canvasHost.port</code>（默认 <code>18793</code>）上启动 Canvas 文件服务器，从 <code>~/.opensoul/workspace/canvas</code> 提供 <code>http://&lt;gateway-host&gt;:18793/__opensoul__/canvas/</code>。使用 <code>canvasHost.enabled=false</code> 或 <code>OPENSOUL_SKIP_CANVAS_HOST=1</code> 禁用。</li><li>输出日志到 stdout；使用 launchd/systemd 保持运行并轮转日志。</li><li>故障排除时传递 <code>--verbose</code> 以将调试日志（握手、请求/响应、事件）从日志文件镜像到 stdio。</li><li><code>--force</code> 使用 <code>lsof</code> 查找所选端口上的监听器，发送 SIGTERM，记录它终止了什么，然后启动 Gateway 网关（如果缺少 <code>lsof</code> 则快速失败）。</li><li>如果你在 supervisor（launchd/systemd/mac 应用子进程模式）下运行，stop/restart 通常发送 <strong>SIGTERM</strong>；旧版本可能将其显示为 <code>pnpm</code> <code>ELIFECYCLE</code> 退出码 <strong>143</strong>（SIGTERM），这是正常关闭，不是崩溃。</li><li><strong>SIGUSR1</strong> 在授权时触发进程内重启（Gateway 网关工具/配置应用/更新，或启用 <code>commands.restart</code> 以进行手动重启）。</li><li>默认需要 Gateway 网关认证：设置 <code>gateway.auth.token</code>（或 <code>OPENSOUL_GATEWAY_TOKEN</code>）或 <code>gateway.auth.password</code>。客户端必须发送 <code>connect.params.auth.token/password</code>，除非使用 Tailscale Serve 身份。</li><li>向导现在默认生成令牌，即使在 loopback 上也是如此。</li><li>端口优先级：<code>--port</code> &gt; <code>OPENSOUL_GATEWAY_PORT</code> &gt; <code>gateway.port</code> &gt; 默认 <code>18789</code>。</li></ul><h2 id="远程访问" tabindex="-1"><a class="header-anchor" href="#远程访问">#</a> 远程访问</h2><ul><li>首选 Tailscale/VPN；否则使用 SSH 隧道：<div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -N</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -L</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 18789:127.0.0.1:18789</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> user@host</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li>然后客户端通过隧道连接到 <code>ws://127.0.0.1:18789</code>。</li><li>如果配置了令牌，即使通过隧道，客户端也必须在 <code>connect.params.auth.token</code> 中包含它。</li></ul><h2 id="多个-gateway-网关-同一主机" tabindex="-1"><a class="header-anchor" href="#多个-gateway-网关-同一主机">#</a> 多个 Gateway 网关（同一主机）</h2><p>通常不需要：一个 Gateway 网关可以服务多个消息渠道和智能体。仅在需要冗余或严格隔离（例如：救援机器人）时使用多个 Gateway 网关。</p><p>如果你隔离状态 + 配置并使用唯一端口，则支持。完整指南：<a href="/docs/gateway/multiple-gateways">多个 Gateway 网关</a>。</p><p>服务名称是配置文件感知的：</p><ul><li>macOS：<code>ai.opensoul.&lt;profile&gt;</code>（旧版 <code>com.opensoul.*</code> 可能仍然存在）</li><li>Linux：<code>opensoul-gateway-&lt;profile&gt;.service</code></li><li>Windows：<code>OpenSoul Gateway (&lt;profile&gt;)</code></li></ul><p>安装元数据嵌入在服务配置中：</p><ul><li><code>OPENSOUL_SERVICE_MARKER=opensoul</code></li><li><code>OPENSOUL_SERVICE_KIND=gateway</code></li><li><code>OPENSOUL_SERVICE_VERSION=&lt;version&gt;</code></li></ul><p>救援机器人模式：保持第二个 Gateway 网关隔离，使用自己的配置文件、状态目录、工作区和基础端口间隔。完整指南：<a href="/docs/gateway/multiple-gateways#rescue-bot-guide">救援机器人指南</a>。</p><h3 id="dev-配置文件-dev" tabindex="-1"><a class="header-anchor" href="#dev-配置文件-dev">#</a> Dev 配置文件（<code>--dev</code>）</h3><p>快速路径：运行完全隔离的 dev 实例（配置/状态/工作区）而不触及你的主设置。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> setup</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --allow-unconfigured</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 然后定位到 dev 实例：</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> health</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>默认值（可通过 env/flags/config 覆盖）：</p><ul><li><code>OPENSOUL_STATE_DIR=~/.opensoul-dev</code></li><li><code>OPENSOUL_CONFIG_PATH=~/.opensoul-dev/opensoul.json</code></li><li><code>OPENSOUL_GATEWAY_PORT=19001</code>（Gateway 网关 WS + HTTP）</li><li>浏览器控制服务端口 = <code>19003</code>（派生：<code>gateway.port+2</code>，仅 loopback）</li><li><code>canvasHost.port=19005</code>（派生：<code>gateway.port+4</code>）</li><li>当你在 <code>--dev</code> 下运行 <code>setup</code>/<code>onboard</code> 时，<code>agents.defaults.workspace</code> 默认变为 <code>~/.opensoul/workspace-dev</code>。</li></ul><p>派生端口（经验法则）：</p><ul><li>基础端口 = <code>gateway.port</code>（或 <code>OPENSOUL_GATEWAY_PORT</code> / <code>--port</code>）</li><li>浏览器控制服务端口 = 基础 + 2（仅 loopback）</li><li><code>canvasHost.port = 基础 + 4</code>（或 <code>OPENSOUL_CANVAS_HOST_PORT</code> / 配置覆盖）</li><li>浏览器配置文件 CDP 端口从 <code>browser.controlPort + 9 .. + 108</code> 自动分配（按配置文件持久化）。</li></ul><p>每个实例的检查清单：</p><ul><li>唯一的 <code>gateway.port</code></li><li>唯一的 <code>OPENSOUL_CONFIG_PATH</code></li><li>唯一的 <code>OPENSOUL_STATE_DIR</code></li><li>唯一的 <code>agents.defaults.workspace</code></li><li>单独的 WhatsApp 号码（如果使用 WA）</li></ul><p>按配置文件安装服务：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --profile</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> main</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --profile</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rescue</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>示例：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OPENSOUL_CONFIG_PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">~/.opensoul/a.json</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> OPENSOUL_STATE_DIR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">~/.opensoul-a</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --port</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 19001</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OPENSOUL_CONFIG_PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">~/.opensoul/b.json</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> OPENSOUL_STATE_DIR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">~/.opensoul-b</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --port</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 19002</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h2 id="协议-运维视角" tabindex="-1"><a class="header-anchor" href="#协议-运维视角">#</a> 协议（运维视角）</h2><ul><li>完整文档：<a href="/docs/gateway/protocol">Gateway 网关协议</a> 和 <a href="/docs/gateway/bridge-protocol">Bridge 协议（旧版）</a>。</li><li>客户端必须发送的第一帧：<code>req {type:&quot;req&quot;, id, method:&quot;connect&quot;, params:{minProtocol,maxProtocol,client:{id,displayName?,version,platform,deviceFamily?,modelIdentifier?,mode,instanceId?}, caps, auth?, locale?, userAgent? } }</code>。</li><li>Gateway 网关回复 <code>res {type:&quot;res&quot;, id, ok:true, payload:hello-ok }</code>（或 <code>ok:false</code> 带错误，然后关闭）。</li><li>握手后： <ul><li>请求：<code>{type:&quot;req&quot;, id, method, params}</code> → <code>{type:&quot;res&quot;, id, ok, payload|error}</code></li><li>事件：<code>{type:&quot;event&quot;, event, payload, seq?, stateVersion?}</code></li></ul></li><li>结构化 presence 条目：<code>{host, ip, version, platform?, deviceFamily?, modelIdentifier?, mode, lastInputSeconds?, ts, reason?, tags?[], instanceId? }</code>（对于 WS 客户端，<code>instanceId</code> 来自 <code>connect.client.instanceId</code>）。</li><li><code>agent</code> 响应是两阶段的：首先 <code>res</code> 确认 <code>{runId,status:&quot;accepted&quot;}</code>，然后在运行完成后发送最终 <code>res</code> <code>{runId,status:&quot;ok&quot;|&quot;error&quot;,summary}</code>；流式输出作为 <code>event:&quot;agent&quot;</code> 到达。</li></ul><h2 id="方法-初始集" tabindex="-1"><a class="header-anchor" href="#方法-初始集">#</a> 方法（初始集）</h2><ul><li><code>health</code> — 完整健康快照（与 <code>opensoul health --json</code> 形状相同）。</li><li><code>status</code> — 简短摘要。</li><li><code>system-presence</code> — 当前 presence 列表。</li><li><code>system-event</code> — 发布 presence/系统注释（结构化）。</li><li><code>send</code> — 通过活跃渠道发送消息。</li><li><code>agent</code> — 运行智能体轮次（在同一连接上流回事件）。</li><li><code>node.list</code> — 列出已配对 + 当前连接的节点（包括 <code>caps</code>、<code>deviceFamily</code>、<code>modelIdentifier</code>、<code>paired</code>、<code>connected</code> 和广播的 <code>commands</code>）。</li><li><code>node.describe</code> — 描述节点（能力 + 支持的 <code>node.invoke</code> 命令；适用于已配对节点和当前连接的未配对节点）。</li><li><code>node.invoke</code> — 在节点上调用命令（例如 <code>canvas.*</code>、<code>camera.*</code>）。</li><li><code>node.pair.*</code> — 配对生命周期（<code>request</code>、<code>list</code>、<code>approve</code>、<code>reject</code>、<code>verify</code>）。</li></ul><p>另见：<a href="/docs/concepts/presence">Presence</a> 了解 presence 如何产生/去重以及为什么稳定的 <code>client.instanceId</code> 很重要。</p><h2 id="事件" tabindex="-1"><a class="header-anchor" href="#事件">#</a> 事件</h2><ul><li><code>agent</code> — 来自智能体运行的流式工具/输出事件（带 seq 标记）。</li><li><code>presence</code> — presence 更新（带 stateVersion 的增量）推送到所有连接的客户端。</li><li><code>tick</code> — 定期保活/无操作以确认活跃。</li><li><code>shutdown</code> — Gateway 网关正在退出；payload 包括 <code>reason</code> 和可选的 <code>restartExpectedMs</code>。客户端应重新连接。</li></ul><h2 id="webchat-集成" tabindex="-1"><a class="header-anchor" href="#webchat-集成">#</a> WebChat 集成</h2><ul><li>WebChat 是原生 SwiftUI UI，直接与 Gateway 网关 WebSocket 通信以获取历史记录、发送、中止和事件。</li><li>远程使用通过相同的 SSH/Tailscale 隧道；如果配置了 Gateway 网关令牌，客户端在 <code>connect</code> 期间包含它。</li><li>macOS 应用通过单个 WS 连接（共享连接）；它从初始快照填充 presence 并监听 <code>presence</code> 事件以更新 UI。</li></ul><h2 id="类型和验证" tabindex="-1"><a class="header-anchor" href="#类型和验证">#</a> 类型和验证</h2><ul><li>服务器使用 AJV 根据从协议定义发出的 JSON Schema 验证每个入站帧。</li><li>客户端（TS/Swift）消费生成的类型（TS 直接使用；Swift 通过仓库的生成器）。</li><li>协议定义是真实来源；使用以下命令重新生成 schema/模型： <ul><li><code>pnpm protocol:gen</code></li><li><code>pnpm protocol:gen:swift</code></li></ul></li></ul><h2 id="连接快照" tabindex="-1"><a class="header-anchor" href="#连接快照">#</a> 连接快照</h2><ul><li><code>hello-ok</code> 包含带有 <code>presence</code>、<code>health</code>、<code>stateVersion</code> 和 <code>uptimeMs</code> 的 <code>snapshot</code>，以及 <code>policy {maxPayload,maxBufferedBytes,tickIntervalMs}</code>，这样客户端无需额外请求即可立即渲染。</li><li><code>health</code>/<code>system-presence</code> 仍可用于手动刷新，但在连接时不是必需的。</li></ul><h2 id="错误码-res-error-形状" tabindex="-1"><a class="header-anchor" href="#错误码-res-error-形状">#</a> 错误码（res.error 形状）</h2><ul><li>错误使用 <code>{ code, message, details?, retryable?, retryAfterMs? }</code>。</li><li>标准码： <ul><li><code>NOT_LINKED</code> — WhatsApp 未认证。</li><li><code>AGENT_TIMEOUT</code> — 智能体未在配置的截止时间内响应。</li><li><code>INVALID_REQUEST</code> — schema/参数验证失败。</li><li><code>UNAVAILABLE</code> — Gateway 网关正在关闭或依赖项不可用。</li></ul></li></ul><h2 id="保活行为" tabindex="-1"><a class="header-anchor" href="#保活行为">#</a> 保活行为</h2><ul><li><code>tick</code> 事件（或 WS ping/pong）定期发出，以便客户端知道即使没有流量时 Gateway 网关也是活跃的。</li><li>发送/智能体确认保持为单独的响应；不要为发送重载 tick。</li></ul><h2 id="重放-间隙" tabindex="-1"><a class="header-anchor" href="#重放-间隙">#</a> 重放 / 间隙</h2><ul><li>事件不会重放。客户端检测 seq 间隙，应在继续之前刷新（<code>health</code> + <code>system-presence</code>）。WebChat 和 macOS 客户端现在会在间隙时自动刷新。</li></ul><h2 id="监管-macos-示例" tabindex="-1"><a class="header-anchor" href="#监管-macos-示例">#</a> 监管（macOS 示例）</h2><ul><li>使用 launchd 保持服务存活： <ul><li>Program：<code>opensoul</code> 的路径</li><li>Arguments：<code>gateway</code></li><li>KeepAlive：true</li><li>StandardOut/Err：文件路径或 <code>syslog</code></li></ul></li><li>失败时，launchd 重启；致命的配置错误应保持退出，以便运维人员注意到。</li><li>LaunchAgents 是按用户的，需要已登录的会话；对于无头设置，使用自定义 LaunchDaemon（未随附）。 <ul><li><code>opensoul gateway install</code> 写入 <code>~/Library/LaunchAgents/ai.opensoul.gateway.plist</code> （或 <code>ai.opensoul.&lt;profile&gt;.plist</code>；旧版 <code>com.opensoul.*</code> 会被清理）。</li><li><code>opensoul doctor</code> 审计 LaunchAgent 配置，可以将其更新为当前默认值。</li></ul></li></ul><h2 id="gateway-网关服务管理-cli" tabindex="-1"><a class="header-anchor" href="#gateway-网关服务管理-cli">#</a> Gateway 网关服务管理（CLI）</h2><p>使用 Gateway 网关 CLI 进行 install/start/stop/restart/status：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stop</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restart</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --follow</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>注意事项：</p><ul><li><code>gateway status</code> 默认使用服务解析的端口/配置探测 Gateway 网关 RPC（使用 <code>--url</code> 覆盖）。</li><li><code>gateway status --deep</code> 添加系统级扫描（LaunchDaemons/系统单元）。</li><li><code>gateway status --no-probe</code> 跳过 RPC 探测（在网络故障时有用）。</li><li><code>gateway status --json</code> 对脚本是稳定的。</li><li><code>gateway status</code> 将 <strong>supervisor 运行时</strong>（launchd/systemd 运行中）与 <strong>RPC 可达性</strong>（WS 连接 + status RPC）分开报告。</li><li><code>gateway status</code> 打印配置路径 + 探测目标以避免&quot;localhost vs LAN 绑定&quot;混淆和配置文件不匹配。</li><li><code>gateway status</code> 在服务看起来正在运行但端口已关闭时包含最后一行 Gateway 网关错误。</li><li><code>logs</code> 通过 RPC 尾随 Gateway 网关文件日志（无需手动 <code>tail</code>/<code>grep</code>）。</li><li>如果检测到其他类似 Gateway 网关的服务，CLI 会发出警告，除非它们是 OpenSoul 配置文件服务。 我们仍然建议大多数设置<strong>每台机器一个 Gateway 网关</strong>；使用隔离的配置文件/端口进行冗余或救援机器人。参见<a href="/docs/gateway/multiple-gateways">多个 Gateway 网关</a>。 <ul><li>清理：<code>opensoul gateway uninstall</code>（当前服务）和 <code>opensoul doctor</code>（旧版迁移）。</li></ul></li><li><code>gateway install</code> 在已安装时是无操作的；使用 <code>opensoul gateway install --force</code> 重新安装（配置文件/env/路径更改）。</li></ul><p>捆绑的 mac 应用：</p><ul><li>OpenSoul.app 可以捆绑基于 Node 的 Gateway 网关中继并安装标记为 <code>ai.opensoul.gateway</code>（或 <code>ai.opensoul.&lt;profile&gt;</code>；旧版 <code>com.opensoul.*</code> 标签仍能干净卸载）的按用户 LaunchAgent。</li><li>要干净地停止它，使用 <code>opensoul gateway stop</code>（或 <code>launchctl bootout gui/$UID/ai.opensoul.gateway</code>）。</li><li>要重启，使用 <code>opensoul gateway restart</code>（或 <code>launchctl kickstart -k gui/$UID/ai.opensoul.gateway</code>）。 <ul><li><code>launchctl</code> 仅在 LaunchAgent 已安装时有效；否则先使用 <code>opensoul gateway install</code>。</li><li>运行命名配置文件时，将标签替换为 <code>ai.opensoul.&lt;profile&gt;</code>。</li></ul></li></ul><h2 id="监管-systemd-用户单元" tabindex="-1"><a class="header-anchor" href="#监管-systemd-用户单元">#</a> 监管（systemd 用户单元）</h2><p>OpenSoul 在 Linux/WSL2 上默认安装 <strong>systemd 用户服务</strong>。我们 建议单用户机器使用用户服务（更简单的 env，按用户配置）。 对于多用户或常驻服务器使用<strong>系统服务</strong>（无需 lingering， 共享监管）。</p><p><code>opensoul gateway install</code> 写入用户单元。<code>opensoul doctor</code> 审计 单元并可以将其更新以匹配当前推荐的默认值。</p><p>创建 <code>~/.config/systemd/user/opensoul-gateway[-&lt;profile&gt;].service</code>：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[Unit]</span></span>
<span class="line"><span>Description=OpenSoul Gateway (profile: &lt;profile&gt;, v&lt;version&gt;)</span></span>
<span class="line"><span>After=network-online.target</span></span>
<span class="line"><span>Wants=network-online.target</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[Service]</span></span>
<span class="line"><span>ExecStart=/usr/local/bin/opensoul gateway --port 18789</span></span>
<span class="line"><span>Restart=always</span></span>
<span class="line"><span>RestartSec=5</span></span>
<span class="line"><span>Environment=OPENSOUL_GATEWAY_TOKEN=</span></span>
<span class="line"><span>WorkingDirectory=/home/youruser</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[Install]</span></span>
<span class="line"><span>WantedBy=default.target</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><p>启用 lingering（必需，以便用户服务在登出/空闲后继续存活）：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sudo loginctl enable-linger youruser</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>新手引导在 Linux/WSL2 上运行此命令（可能提示输入 sudo；写入 <code>/var/lib/systemd/linger</code>）。 然后启用服务：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>systemctl --user enable --now opensoul-gateway[-&lt;profile&gt;].service</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong>替代方案（系统服务）</strong> - 对于常驻或多用户服务器，你可以 安装 systemd <strong>系统</strong>单元而不是用户单元（无需 lingering）。 创建 <code>/etc/systemd/system/opensoul-gateway[-&lt;profile&gt;].service</code>（复制上面的单元， 切换 <code>WantedBy=multi-user.target</code>，设置 <code>User=</code> + <code>WorkingDirectory=</code>），然后：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sudo systemctl daemon-reload</span></span>
<span class="line"><span>sudo systemctl enable --now opensoul-gateway[-&lt;profile&gt;].service</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h2 id="windows-wsl2" tabindex="-1"><a class="header-anchor" href="#windows-wsl2">#</a> Windows（WSL2）</h2><p>Windows 安装应使用 <strong>WSL2</strong> 并遵循上面的 Linux systemd 部分。</p><h2 id="运维检查" tabindex="-1"><a class="header-anchor" href="#运维检查">#</a> 运维检查</h2><ul><li>存活检查：打开 WS 并发送 <code>req:connect</code> → 期望收到带有 <code>payload.type=&quot;hello-ok&quot;</code>（带快照）的 <code>res</code>。</li><li>就绪检查：调用 <code>health</code> → 期望 <code>ok: true</code> 并在 <code>linkChannel</code> 中有已关联的渠道（适用时）。</li><li>调试：订阅 <code>tick</code> 和 <code>presence</code> 事件；确保 <code>status</code> 显示已关联/认证时间；presence 条目显示 Gateway 网关主机和已连接的客户端。</li></ul><h2 id="安全保证" tabindex="-1"><a class="header-anchor" href="#安全保证">#</a> 安全保证</h2><ul><li>默认假设每台主机一个 Gateway 网关；如果你运行多个配置文件，隔离端口/状态并定位到正确的实例。</li><li>不会回退到直接 Baileys 连接；如果 Gateway 网关关闭，发送会快速失败。</li><li>非 connect 的第一帧或格式错误的 JSON 会被拒绝并关闭 socket。</li><li>优雅关闭：关闭前发出 <code>shutdown</code> 事件；客户端必须处理关闭 + 重新连接。</li></ul><h2 id="cli-辅助工具" tabindex="-1"><a class="header-anchor" href="#cli-辅助工具">#</a> CLI 辅助工具</h2><ul><li><code>opensoul gateway health|status</code> — 通过 Gateway 网关 WS 请求 health/status。</li><li><code>opensoul message send --target &lt;num&gt; --message &quot;hi&quot; [--media ...]</code> — 通过 Gateway 网关发送（对 WhatsApp 是幂等的）。</li><li><code>opensoul agent --message &quot;hi&quot; --to &lt;num&gt;</code> — 运行智能体轮次（默认等待最终结果）。</li><li><code>opensoul gateway call &lt;method&gt; --params &#39;{&quot;k&quot;:&quot;v&quot;}&#39;</code> — 用于调试的原始方法调用器。</li><li><code>opensoul gateway stop|restart</code> — 停止/重启受监管的 Gateway 网关服务（launchd/systemd）。</li><li>Gateway 网关辅助子命令假设 <code>--url</code> 上有运行中的 Gateway 网关；它们不再自动生成一个。</li></ul><h2 id="迁移指南" tabindex="-1"><a class="header-anchor" href="#迁移指南">#</a> 迁移指南</h2><ul><li>淘汰 <code>opensoul gateway</code> 和旧版 TCP 控制端口的使用。</li><li>更新客户端以使用带有强制 connect 和结构化 presence 的 WS 协议。</li></ul>`,79)])])}const g=s(n,[["render",o]]);export{u as __pageData,g as default};
