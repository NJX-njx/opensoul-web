import{_ as s,o as a,c as i,ai as t}from"./chunks/framework.CsYGeNYI.js";const g=JSON.parse('{"title":"gateway","description":"","frontmatter":{"summary":"OpenSoul Gateway CLI (`opensoul gateway`) — run, query, and discover gateways","read_when":["Running the Gateway from the CLI (dev or servers)","Debugging Gateway auth, bind modes, and connectivity","Discovering gateways via Bonjour (LAN + tailnet)"],"title":"gateway"},"headers":[],"relativePath":"cli/gateway.md","filePath":"cli/gateway.md","lastUpdated":1771906665000}'),l={name:"cli/gateway.md"};function o(n,e,d,p,r,c){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="gateway-cli" tabindex="-1"><a class="header-anchor" href="#gateway-cli">#</a> Gateway CLI</h1><p>The Gateway is OpenSoul’s WebSocket server (channels, nodes, sessions, hooks).</p><p>Subcommands in this page live under <code>opensoul gateway …</code>.</p><p>Related docs:</p><ul><li><a href="/docs/gateway/bonjour">/gateway/bonjour</a></li><li><a href="/docs/gateway/discovery">/gateway/discovery</a></li><li><a href="/docs/gateway/configuration">/gateway/configuration</a></li></ul><h2 id="run-the-gateway" tabindex="-1"><a class="header-anchor" href="#run-the-gateway">#</a> Run the Gateway</h2><p>Run a local Gateway process:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Foreground alias:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Notes:</p><ul><li>By default, the Gateway refuses to start unless <code>gateway.mode=local</code> is set in <code>~/.opensoul/opensoul.json</code>. Use <code>--allow-unconfigured</code> for ad-hoc/dev runs.</li><li>Binding beyond loopback without auth is blocked (safety guardrail).</li><li><code>SIGUSR1</code> triggers an in-process restart when authorized (enable <code>commands.restart</code> or use the gateway tool/config apply/update).</li><li><code>SIGINT</code>/<code>SIGTERM</code> handlers stop the gateway process, but they don’t restore any custom terminal state. If you wrap the CLI with a TUI or raw-mode input, restore the terminal before exit.</li></ul><h3 id="options" tabindex="-1"><a class="header-anchor" href="#options">#</a> Options</h3><ul><li><code>--port &lt;port&gt;</code>: WebSocket port (default comes from config/env; usually <code>18789</code>).</li><li><code>--bind &lt;loopback|lan|tailnet|auto|custom&gt;</code>: listener bind mode.</li><li><code>--auth &lt;token|password&gt;</code>: auth mode override.</li><li><code>--token &lt;token&gt;</code>: token override (also sets <code>OPENSOUL_GATEWAY_TOKEN</code> for the process).</li><li><code>--password &lt;password&gt;</code>: password override (also sets <code>OPENSOUL_GATEWAY_PASSWORD</code> for the process).</li><li><code>--tailscale &lt;off|serve|funnel&gt;</code>: expose the Gateway via Tailscale.</li><li><code>--tailscale-reset-on-exit</code>: reset Tailscale serve/funnel config on shutdown.</li><li><code>--allow-unconfigured</code>: allow gateway start without <code>gateway.mode=local</code> in config.</li><li><code>--dev</code>: create a dev config + workspace if missing (skips BOOTSTRAP.md).</li><li><code>--reset</code>: reset dev config + credentials + sessions + workspace (requires <code>--dev</code>).</li><li><code>--force</code>: kill any existing listener on the selected port before starting.</li><li><code>--verbose</code>: verbose logs.</li><li><code>--claude-cli-logs</code>: only show claude-cli logs in the console (and enable its stdout/stderr).</li><li><code>--ws-log &lt;auto|full|compact&gt;</code>: websocket log style (default <code>auto</code>).</li><li><code>--compact</code>: alias for <code>--ws-log compact</code>.</li><li><code>--raw-stream</code>: log raw model stream events to jsonl.</li><li><code>--raw-stream-path &lt;path&gt;</code>: raw stream jsonl path.</li></ul><h2 id="query-a-running-gateway" tabindex="-1"><a class="header-anchor" href="#query-a-running-gateway">#</a> Query a running Gateway</h2><p>All query commands use WebSocket RPC.</p><p>Output modes:</p><ul><li>Default: human-readable (colored in TTY).</li><li><code>--json</code>: machine-readable JSON (no styling/spinner).</li><li><code>--no-color</code> (or <code>NO_COLOR=1</code>): disable ANSI while keeping human layout.</li></ul><p>Shared options (where supported):</p><ul><li><code>--url &lt;url&gt;</code>: Gateway WebSocket URL.</li><li><code>--token &lt;token&gt;</code>: Gateway token.</li><li><code>--password &lt;password&gt;</code>: Gateway password.</li><li><code>--timeout &lt;ms&gt;</code>: timeout/budget (varies per command).</li><li><code>--expect-final</code>: wait for a “final” response (agent calls).</li></ul><p>Note: when you set <code>--url</code>, the CLI does not fall back to config or environment credentials. Pass <code>--token</code> or <code>--password</code> explicitly. Missing explicit credentials is an error.</p><h3 id="gateway-health" tabindex="-1"><a class="header-anchor" href="#gateway-health">#</a> <code>gateway health</code></h3><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> health</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --url</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ws://127.0.0.1:18789</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h3 id="gateway-status" tabindex="-1"><a class="header-anchor" href="#gateway-status">#</a> <code>gateway status</code></h3><p><code>gateway status</code> shows the Gateway service (launchd/systemd/schtasks) plus an optional RPC probe.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --json</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Options:</p><ul><li><code>--url &lt;url&gt;</code>: override the probe URL.</li><li><code>--token &lt;token&gt;</code>: token auth for the probe.</li><li><code>--password &lt;password&gt;</code>: password auth for the probe.</li><li><code>--timeout &lt;ms&gt;</code>: probe timeout (default <code>10000</code>).</li><li><code>--no-probe</code>: skip the RPC probe (service-only view).</li><li><code>--deep</code>: scan system-level services too.</li></ul><h3 id="gateway-probe" tabindex="-1"><a class="header-anchor" href="#gateway-probe">#</a> <code>gateway probe</code></h3><p><code>gateway probe</code> is the “debug everything” command. It always probes:</p><ul><li>your configured remote gateway (if set), and</li><li>localhost (loopback) <strong>even if remote is configured</strong>.</li></ul><p>If multiple gateways are reachable, it prints all of them. Multiple gateways are supported when you use isolated profiles/ports (e.g., a rescue bot), but most installs still run a single gateway.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> probe</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> probe</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --json</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h4 id="remote-over-ssh-mac-app-parity" tabindex="-1"><a class="header-anchor" href="#remote-over-ssh-mac-app-parity">#</a> Remote over SSH (Mac app parity)</h4><p>The macOS app “Remote over SSH” mode uses a local port-forward so the remote gateway (which may be bound to loopback only) becomes reachable at <code>ws://127.0.0.1:&lt;port&gt;</code>.</p><p>CLI equivalent:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> probe</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ssh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> user@gateway-host</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Options:</p><ul><li><code>--ssh &lt;target&gt;</code>: <code>user@host</code> or <code>user@host:port</code> (port defaults to <code>22</code>).</li><li><code>--ssh-identity &lt;path&gt;</code>: identity file.</li><li><code>--ssh-auto</code>: pick the first discovered gateway host as SSH target (LAN/WAB only).</li></ul><p>Config (optional, used as defaults):</p><ul><li><code>gateway.remote.sshTarget</code></li><li><code>gateway.remote.sshIdentity</code></li></ul><h3 id="gateway-call-method" tabindex="-1"><a class="header-anchor" href="#gateway-call-method">#</a> <code>gateway call &lt;method&gt;</code></h3><p>Low-level RPC helper.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> call</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> call</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs.tail</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --params</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;sinceMs&quot;: 60000}&#39;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h2 id="manage-the-gateway-service" tabindex="-1"><a class="header-anchor" href="#manage-the-gateway-service">#</a> Manage the Gateway service</h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stop</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restart</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> uninstall</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Notes:</p><ul><li><code>gateway install</code> supports <code>--port</code>, <code>--runtime</code>, <code>--token</code>, <code>--force</code>, <code>--json</code>.</li><li>Lifecycle commands accept <code>--json</code> for scripting.</li></ul><h2 id="discover-gateways-bonjour" tabindex="-1"><a class="header-anchor" href="#discover-gateways-bonjour">#</a> Discover gateways (Bonjour)</h2><p><code>gateway discover</code> scans for Gateway beacons (<code>_opensoul-gw._tcp</code>).</p><ul><li>Multicast DNS-SD: <code>local.</code></li><li>Unicast DNS-SD (Wide-Area Bonjour): choose a domain (example: <code>opensoul.internal.</code>) and set up split DNS + a DNS server; see <a href="/docs/gateway/bonjour">/gateway/bonjour</a></li></ul><p>Only gateways with Bonjour discovery enabled (default) advertise the beacon.</p><p>Wide-Area discovery records include (TXT):</p><ul><li><code>role</code> (gateway role hint)</li><li><code>transport</code> (transport hint, e.g. <code>gateway</code>)</li><li><code>gatewayPort</code> (WebSocket port, usually <code>18789</code>)</li><li><code>sshPort</code> (SSH port; defaults to <code>22</code> if not present)</li><li><code>tailnetDns</code> (MagicDNS hostname, when available)</li><li><code>gatewayTls</code> / <code>gatewayTlsSha256</code> (TLS enabled + cert fingerprint)</li><li><code>cliPath</code> (optional hint for remote installs)</li></ul><h3 id="gateway-discover" tabindex="-1"><a class="header-anchor" href="#gateway-discover">#</a> <code>gateway discover</code></h3><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> discover</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Options:</p><ul><li><code>--timeout &lt;ms&gt;</code>: per-command timeout (browse/resolve); default <code>2000</code>.</li><li><code>--json</code>: machine-readable output (also disables styling/spinner).</li></ul><p>Examples:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> discover</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --timeout</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4000</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opensoul</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> discover</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --json</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;.beacons[].wsUrl&#39;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div>`,60)])])}const u=s(l,[["render",o]]);export{g as __pageData,u as default};
